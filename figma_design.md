# 云登客户端 Figma 设计规范

> 本文是云登 / YunLogin PC 端 HTML 原型同步到 Figma 时的设计稿构建与交付规范。
> 它记录 Figma 文件中已核实的组件组织方式，并规定如何将项目现行视觉 Token、SystemFrame 架构和页面状态映射到 Figma。
>
> **视觉实现的单一事实来源仍是 [`design.md`](./design.md)**；本文件不是第二套颜色、字号或圆角规范。Figma 原稿中存在一批历史组件值，本文会单独标注，避免新稿继续复制历史偏差。

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 适用产品 | 云登客户端 3.0 / YunLogin PC 端 |
| 参考文件 | [云登客户端3.0 – Figma](https://www.figma.com/design/tKH6mQcxRJiPGEuyy7aUT6/%E4%BA%91%E7%99%BB%E5%AE%A2%E6%88%B7%E7%AB%AF3.0?node-id=0-1) |
| 本次核对 | 2026-08-24，使用只读/评论权限检查组件页 |
| 实现规范 | [`design.md`](./design.md) |
| 页面与业务规则 | 对应模块 `PRD/*.md`，系统外壳参考 [`PRD/系统框架PRD.md`](./PRD/系统框架PRD.md) |
| 原型入口 | `index.html?page=<key>` |
| 主题 | 浅色主题；未定义独立深色主题 |
| 本次 Figma 操作 | 仅查看，未修改原文件，未提交编辑权限申请 |

### 0.1 规范优先级

制作或同步页面时按以下顺序处理冲突：

1. 用户当前明确需求；
2. [`AGENTS.md`](./AGENTS.md) 的工程与交付约束；
3. 已核实的运行时行为与目标模块 PRD；
4. [`design.md`](./design.md) 的现行视觉 Token；
5. 本文件中对 Figma 组件的组织和落稿建议；
6. 未经核实的推断。

当 Figma 原稿和 `design.md` 不一致时：

- **新建或改版页面**使用 `design.md` 的现行值，并在 Figma 中绑定项目变量；
- **还原既有历史页面**可以暂时保留 Figma 原值，但必须在页面说明中标记“历史还原”，不能把它扩散为全局组件默认值；
- 无法从 Figma 只读面板确认的值标记为“待确认”，不通过目测或截图猜测精确 Token。

## 1. Figma 文件结构

### 1.1 已观察到的原文件页面

参考文件包含业务页面、流程页面和组件页面。与前端落稿最相关的页面如下：

- `组件`：基础组件、状态示例和弹窗案例集合；
- `费用管理`：订单、开票等费用业务场景；
- `环境管理`、`代理管理`、`插件管理`、`团队管理`、`账号管理`：典型列表、筛选、表格和详情场景；
- `客服入口、空状态`：客服入口、空状态和缺省反馈场景；
- `新手指引`、`卸载原因`、`安装`：引导及流程型弹层场景；
- `云登logo`、`图标格式替换`：品牌和图标资源。

页面名称保留中文，业务页面不要另建一套英文页面名或英文标题。

### 1.2 组件页的实际分组

组件页当前可见的主要分组为：

| 分组 | 已观察到的组件命名 | 用途 |
| --- | --- | --- |
| 头部 | `头部`、`头部/浏览器1336`、`头部/浏览器1920`、`头部/客户端` | 浏览器式外壳、客户端头部、不同视口示例 |
| 输入框 | `输入框`、`输入框/新建/选择`、`输入框/弹窗/日期`、`输入框/弹窗/时间`、`输入框/弹窗/选择`、`输入框/弹窗/搜索`、`输入框/弹窗/输入`、`输入框/弹窗/数量`、`输入框/弹窗/多文本输入`、`输入框/表单/日期`、`输入框/表单/选择`、`输入框/表单/搜索` | 输入、选择、搜索、日期、时间、数量和多行文本 |
| 导航 | `导航`、`导航/侧边导航`、`导航/侧边导航/按钮`、`导航/侧边导航/一级描述`、`导航/侧边导航/二级描述`、`导航/TAB导航` | 侧边导航、折叠态、二级菜单和页内 Tab |
| 按钮 | `按钮`、`按钮/32高/圆角3`、`按钮/28高/圆角6`、`按钮/40高/圆角6`、`按钮/更多操作`、`按钮/更多操作表单`、`按钮/新建浏览器/圆角3`、`按钮/开关`、`按钮/多选`、`按钮/单选` | 操作按钮、图标按钮、开关、复选和单选 |
| 表单 | `表单`、`表单/列表字段`、`表单/列表字段/代理类型标签`、`表单/表背景`、`表单/置弹窗`、`表单/分页` | 表格字段、业务标签、分页和弹窗表单 |
| 图标 | `icons`、`图标/头部`、`图标/TAB图标`、`图标/平台/*` | 系统图标、平台图标和头部操作图标 |
| 提示 | 组件页中的“提示”分组及弹窗案例区域 | Dialog、Toast、错误和空状态 |
| 其他 | 组件页中的“其他”分组 | 不适合归入上述类别的业务小组件 |

### 1.3 推荐的新文件页面结构

以下是后续整理新设计文件时的建议结构，不代表对现有文件的强制改名：

```text
00 说明与变更记录
01 基础变量
02 组件
03 页面模板
10 业务页面
90 状态与流程
99 归档
```

建议把可复用组件集中在 `02 组件`，把完整页面实例放在 `10 业务页面`；不要在业务页面中复制一份“看起来一样但没有关联”的按钮、输入框或弹窗。

### 1.4 SystemFrame 与 ModuleFrame 的 Figma 边界

项目后台只有一个 SystemFrame。Figma 页面也必须保持同样的所有权边界：

```text
SystemFrame
├─ BrowserChrome
├─ TopBar
├─ Sidebar
├─ GlobalAssistant（如页面存在）
├─ GlobalToast / GlobalPopover / GlobalDialog / GlobalDrawer
└─ RouterOutlet
   └─ ModuleFrame
      ├─ 业务筛选区
      ├─ 业务数据区
      └─ 业务弹窗、业务抽屉、业务空状态
```

- 完整页面稿可以包含 SystemFrame 和 ModuleFrame；
- 业务模块稿只放 Router Outlet 内的 ModuleFrame，不复制顶栏、侧栏和全局助手；
- 需要展示全局 Drawer 或全局 Toast 时，在 `90 状态与流程` 单独放一张带外壳的状态稿，并在图层说明归属；
- Figma 中的遮罩、弹窗和抽屉必须是状态层或独立状态 Frame，不能把它们永久叠在默认页面上而造成误解。

## 2. 基础变量与视觉 Token

### 2.1 颜色变量

下表是新稿应绑定的项目变量。变量名建议使用英文稳定 ID，变量描述和 Figma 图层名使用中文；这不会把英文显示给最终用户。

| 变量建议名 | 当前值 | 语义 |
| --- | --- | --- |
| `color/primary` | `#0066FF` | 主操作、选中、链接、品牌强调 |
| `color/primary-hover` | `#0052CC` | 主按钮悬停 |
| `color/primary-active` | `#0047B3` | 主按钮按下 |
| `color/primary-bg` | `#E6F0FF` | 主色浅底、选中背景 |
| `color/ink-title` | `#1A1D24` | 页面和区块标题 |
| `color/ink-body` | `#3A3F4A` | 正文、表格内容 |
| `color/ink-sub` | `#6E7685` | 辅助文字、表头 |
| `color/ink-muted` | `#9DA2AC` | 占位、禁用、空状态辅助文字 |
| `color/line` | `#DFE1E5` | 主边框和分割线 |
| `color/line-light` | `#E8EAED` | 浅分割线 |
| `color/line-lighter` | `#F0F1F3` | 表头底、极浅分隔 |
| `color/bg-page` | `#F7F8FA` | 页面背景 |
| `color/bg-card` | `#FFFFFF` | 卡片、弹窗、输入控件背景 |
| `color/bg-hover` | `#F3F4F6` | 行、菜单和普通项悬停 |
| `color/block-hover` | `#F0F1F3` | 白色业务区块悬停 |
| `color/success` / `color/success-bg` | `#0FC060` / `#E7F9F0` | 成功、进行中、完成 |
| `color/warning` / `color/warning-bg` | `#E7772D` / `#FDF2E9` | 警告、临期、超时 |
| `color/danger` / `color/danger-bg` | `#D9001B` / `#FFE8EB` | 错误、异常、删除 |
| `color/info` / `color/info-bg` | `#0091D5` / `#E4F4FB` | 信息、中性提示 |
| `color/price` | `#FE5416` | 商品销售价格，仅用于价格数字 |

### 2.2 Figma 原稿中的历史颜色

组件页只读面板中核实到以下历史值：

| 历史值 | 出现位置 | 处理方式 |
| --- | --- | --- |
| `#2D5AFF` | 旧组件主色、边框样式 | 仅用于严格还原旧稿；新稿改绑 `color/primary` |
| `#577BFF` | 旧主按钮悬停 | 新稿使用 `color/primary-hover` |
| `#9DB2FF` | 旧主按钮禁用 | 新稿使用 `color/ink-muted` 或对应禁用浅底 |
| `#EBEBEB` | 旧组件边框 | 新稿使用 `color/line` 或 `color/line-light` |
| `#FAFAFA` | 旧输入框背景 | 新稿通常使用 `color/bg-card`；若模块明确要求表头/输入浅底，需在页面说明中记录 |
| `#F4F7FF` | 旧组件展示板背景 | 这是组件展示板背景，不是业务页面背景 |
| `#333333` | 旧按钮正文 | 新稿使用 `color/ink-body` |

这些值不能直接写入新的全局变量。若必须还原历史页面，建议建立 `legacy/云登3.0旧组件` 变量组，并在页面说明中标明迁移计划。

### 2.3 字体与文字样式

#### 项目现行字体

- UI 文本：`-apple-system, BlinkMacSystemFont, PingFang SC, Hiragino Sans GB, Microsoft YaHei, Helvetica Neue, Helvetica, Arial, sans-serif`；
- 数字、订单号、金额、编码和时间戳：`JetBrains Mono`；
- Figma 中不要把业务中文文字转换为路径；必须保留可编辑文本。

#### Figma 原稿中已观察到的文字样式

| Figma 样式名 | 尺寸 / 行高 | 观察结果 | 新稿映射 |
| --- | --- | --- | --- |
| `正文` | `14/18` | `Source Han Sans SC`、常规、字距 0 | 页面控件需要 14px 时使用；一般列表正文仍服从 `design.md` 的 13px |
| `标题` | `16/20` | 组件标题级别 | 映射到区块标题或组件标题 |
| `辅助文字` | `12/16` | 次要说明 | 映射到辅助文字 |
| `辅助文字突出` | `12/16` | 强调型辅助信息 | 仅在语义确有强调时使用 |
| `正文突出文字` | `14/18` | 正文强调 | 不通过加大字号制造强调 |
| `大标题` | `18/26` | 页面或大区块标题 | 页面实际层级由 `design.md` 和模块 PRD 决定 |

新稿至少建立以下 Figma 文本样式映射：

```text
文字/页面标题
文字/区块标题
文字/正文
文字/表格正文
文字/辅助
文字/数字与编号
```

字号、字重和行高都必须从 `design.md` 或目标 PRD 取值。不要因为 Figma 右侧显示了历史 `14/18` 就把整个后台表格改成 14px。

### 2.4 间距、圆角和阴影

- 间距基数为 6px；常用值为 `4 / 6 / 8 / 10 / 12 / 16 / 20 / 32px`；
- 新稿默认小组件圆角 `4px`，卡片/Modal/大容器 `8px`，大型引导 Modal 仅按 PRD 使用 `10px`；
- 阴影优先用边框和底色表达层级；轻阴影为 `0 2px 8px rgba(0,0,0,.06)`，中阴影为 `0 6px 24px rgba(0,0,0,.12)`；
- Figma 原稿中的 `3px`、`6px`、`12px` 多为历史组件展示值。除非是在还原对应组件，不要把它们当作新的全局圆角。

### 2.5 图标

- 统一使用 Lucide 线性图标；页面图标不得用 emoji；
- 常用尺寸为 14–16px，头部和全局操作按钮常用 18px；具体尺寸按 `design.md` 和组件规格绑定；
- 图标和文字的语义颜色必须来自颜色变量；
- 业务平台图标、国家图标和品牌图标使用 Figma 资源库中的已审核资源，不要在页面中临时绘制近似图标；
- 图标层必须保留明确命名，例如 `图标/搜索`、`图标/关闭`、`图标/日历`，不要保留 `Vector 123`、`Group 45` 等默认名称。

## 3. 组件规范

### 3.1 组件命名与变体

现有组件使用中文斜杠层级命名，例如 `按钮/32高/圆角3`。后续维护既有组件时保留其名称，以避免实例断链；新建组件建议使用以下结构：

```text
组件类别/语义/尺寸
```

变体属性统一使用以下中文属性名：

| 属性 | 示例值 |
| --- | --- |
| `尺寸` | `小 / 中 / 大` |
| `类型` | `主按钮 / 次按钮 / 文字按钮 / 危险按钮` |
| `状态` | `默认 / 悬停 / 按下 / 聚焦 / 禁用 / 加载` |
| `图标` | `无 / 左侧 / 右侧 / 仅图标` |
| `方向` | `展开 / 收起` |
| `选中` | `是 / 否` |
| `密度` | `标准 / 紧凑` |
| `内容` | `单行 / 多行 / 密码 / 带计数` |

属性只描述可替换的行为或外观，不要把业务文案写进组件名称，例如不要创建 `按钮/订单管理查询按钮`。

### 3.2 头部与 App Shell

Figma 组件页已提供：`头部/浏览器1336`、`头部/浏览器1920`、`头部/客户端`。它们应作为 SystemFrame 的基础实例，而不是业务页面的装饰背景。

#### 现行结构

| 区域 | 现行项目规格 |
| --- | --- |
| BrowserChrome | 标签行 32px + 工具行 52px，总高 84px |
| TopBar | 高 56px；桌面左右内边距 24px，平板 12px |
| Sidebar | 完整态 220px；紧凑态 68px |
| ModuleFrame | 由公共层提供四边 16px 内边距，业务根不再额外收窄整个页面 |

#### Figma 结构要求

- Shell 外壳使用独立 Auto Layout Frame；
- TopBar、Sidebar、BrowserChrome 均使用固定宽/高约束；
- Router Outlet 使用 `Fill container`，不能用手工拉伸的固定宽度；
- 侧栏折叠应通过组件变体展示，不复制两份完全独立的导航；
- 页面设计稿需要同时提供至少一个完整态和一个紧凑态样例；
- 业务模块 Frame 不得包含第二套 BrowserChrome、TopBar 或 Sidebar。

### 3.3 侧边导航

Figma 中已观察到 `导航/侧边导航`、`导航/侧边导航/按钮`、`导航/侧边导航/一级描述`、`导航/侧边导航/二级描述` 等组件。

#### 现行规格

- 一级菜单高 36px，图标 16px，文字 14px；
- 二级菜单高 36px，不显示图标和左侧竖线，文字起始位置与一级文字对齐；
- 完整态显示图标和文字，紧凑态仅保留图标；
- 当前项使用 `primary` 语义，普通项使用 `ink-body`；
- 分组展开/收起使用尾部箭头，不改变图标和标题的起始位置；
- 侧栏自身无外边框、无重阴影；
- 768px 以下改为遮罩抽屉，不再使用 68px 紧凑态。

#### Figma 变体建议

```text
导航/侧边导航/菜单项
  尺寸=标准 | 状态=默认/悬停/选中/禁用 | 层级=一级/二级 | 展开=是/否
导航/侧边导航/容器
  模式=完整/紧凑 | 视口=桌面/平板/移动
```

不要在每个业务页面中复制一份菜单实例。页面稿只替换当前路由选中态，并通过文字和图标实例属性保持一致。

### 3.4 Tab 导航

Figma `导航/TAB导航` 页面展示了多种 Tab 形态：带图标的线性 Tab、纯文字线性 Tab、带计数的 Tab、分段 Tab。

| 形态 | 用途 | Figma 变体 |
| --- | --- | --- |
| 图标线性 | 模块或资源类别切换 | `样式=图标线性` |
| 文字线性 | 页面内二级视图 | `样式=文字线性` |
| 计数 Tab | 需要展示数量的列表分组 | `计数=有` |
| 分段 Tab | 少量互斥模式切换 | `样式=分段` |

共通规则：

- 选中态使用 `primary` 文字和底部指示线或主色底；
- 未选中态使用 `ink-body` / `ink-sub`；
- 禁用态使用 `ink-muted`，不产生 hover；
- Tab 文案使用真实中文业务名，不使用 `TAB001`、`TAB002` 等占位文字；
- 计数是数据的一部分，使用 `JetBrains Mono` 或项目规定的数字样式；
- Tab 过多时横向滚动，不压缩到文字重叠。

### 3.5 按钮

Figma 组件页至少包含 `按钮/32高/圆角3`、`按钮/28高/圆角6`、`按钮/40高/圆角6`、`按钮/更多操作` 和 `按钮/新建浏览器/圆角3`。

#### 项目新稿规格

| 尺寸 | 适用场景 | 现行值 |
| --- | --- | --- |
| 小 | 表格行内操作、分页、紧凑工具条 | 高 28px，圆角 4px |
| 中 | 默认页面操作、筛选查询、普通弹窗操作 | 高 32px，圆角 4px |
| 大 | 模块 PRD 明确的重要 CTA | 高 40px，圆角 4px |

#### Figma 原稿中已核实的 32 高按钮细节

选中 `按钮/32高/圆角3` 的主按钮默认变体时，Figma 面板显示：高 32px、圆角 3px、上下内边距 7px、左右内边距 20px、图标/文字间距 10px；主按钮历史颜色为 `#2D5AFF`。次按钮为白底、1px 边框，悬停边框使用历史主色。

这些数值用于理解旧组件，不覆盖项目新稿规则。新稿应优先使用 `design.md` 的 4px 圆角、primary 色和组件实际上下文间距。

#### 按钮状态矩阵

每个可交互按钮至少准备：

```text
默认 / 悬停 / 按下 / 聚焦 / 禁用
```

异步操作再增加：

```text
加载中（保留按钮宽度，禁止重复点击）
```

规则：

- 主按钮：`primary` 实底 + 白字；
- 次按钮：白底 + `line` 描边 + `ink-body`；
- 危险按钮：`danger` 实底或描边；
- 文字按钮：只在明确的轻操作或表格操作中使用；
- 图标按钮：有 Lucide 图标时不重复放冗余文字；陌生图标必须配 Tooltip；
- 图标和文字的顺序、间距在同一组件集内固定；
- 禁止用圆角矩形包裹“保存”“删除”等文字而省略已有的熟悉图标，具体按操作语义选择图标或图标+文字。

### 3.6 开关、复选框与单选框

Figma 已提供 `按钮/开关`、`按钮/多选`、`按钮/单选`。

| 控件 | 必须具备的变体 |
| --- | --- |
| 开关 | `开/关` × `默认/悬停/禁用`；开态使用 primary，关态使用 line 和浅灰底 |
| 复选框 | `未选/已选/半选` × `默认/悬停/禁用/错误` |
| 单选框 | `未选/已选` × `默认/悬停/禁用/错误` |

控件的点击区域应大于图形本身，Figma 中用 Auto Layout 保留稳定命中区；不要只画一个不可交互的小圆点或小方框。

### 3.7 输入框与选择器

Figma 组件页把输入按使用场景分为弹窗和表单，并展示了常规、悬停、输入、多字段、密码等状态。

#### 输入状态

```text
空值 / 悬停 / 聚焦 / 已输入 / 清空按钮可见 / 密码隐藏 / 密码显示 / 禁用 / 错误
```

带计数或多文本输入再增加：

```text
计数正常 / 接近上限 / 超限
```

#### 尺寸和场景

- 筛选和普通页面控件：按 `design.md` 使用 32px 高度；
- 通用 Modal 表单控件：30px 高度、400px 基准宽度；
- 配置型长表单：32px 高度、500px 基准宽度；
- 订单创建时间范围：一个连续的 300px × 32px 组合控件，不拆成两个可见边框；
- 开票申请：标签列 112px、控件列 400px，具体业务特例以 `design.md` 和 `PRD/费用管理PRD.md` 为准。

#### 标签规则

| 场景 | 标签规则 |
| --- | --- |
| 筛选区 | 标签在控件上方，通常不追加中文冒号 |
| 普通 Modal 表单 | 标签右对齐，末尾使用一个中文全角冒号 |
| 配置型表单 | 默认不显示中文冒号；必填 `*` 放在字段名左侧 |
| 开票申请 | 112px 标签列右对齐，保留一个中文全角冒号；非必填不重复写“选填” |

下拉面板、日期面板、Popover 必须作为触发控件的同一交互组件状态展示。面板打开时父级高度、遮挡关系和响应式位置要在 Figma 中单独核验。

### 3.8 表格、列表字段和状态

Figma 中已提供 `表单/列表字段`、`表单/列表字段/代理类型标签` 和 `表单/表背景`。

#### 表格结构

- 表头使用 `line-lighter` 底色、`ink-sub` 文字、12px 字号；
- 数据行使用 13px 正文，行高约 44–52px，保留每一行底部边框；
- 所有列左对齐，包括数字、金额、订单号和操作列；
- 表格外层在窄屏允许横向滚动；
- 操作列使用固定间距的操作组，不通过逐个链接的 margin 拼接；
- 排序表头必须同时展示字段名和排序方向状态，Figma 需准备未排序、升序、降序三种实例；
- 末行边框不能因为截图裁切、容器裁切或 `last-child` 规则消失。

#### 状态表达

- 高密度订单、开票列表使用语义色纯文本状态；
- 低密度详情和汇总卡片可使用 4px 圆角 Badge；
- 进行中/完成使用 success，临期/超时使用 warning，异常/删除使用 danger，中性信息使用 info；
- 状态文案使用准确中文，不用颜色单独传达状态。

### 3.9 分页

Figma `表单/分页` 展示了总条数、条/页选择、页码导航、省略号和跳页输入。

项目新稿必须遵守 `design.md` 的分页规范：

- 分页属于数据区，不是独立卡片；
- 位于表格右下方，距最后一条数据行底部 20px；
- 页码按钮高 30px、最小宽 30px、圆角 4px；
- 当前页为 primary 实底；
- 首末页箭头在边界禁用；
- `≤7` 页全部显示，超过 7 页使用省略号；
- 条/页下拉与跳页输入高 30px；
- 跳页输入默认值为 1，成功跳转后恢复为 1，不留空；
- 统计文本使用 `ink-muted`，数字可使用 Mono；
- 小屏允许整组换行，不产生页面级横向溢出。

### 3.10 Modal、Dialog、Drawer 与 Popover

组件页中央可见“弹窗案例样式”集合；具体实现值以 `design.md` 为准。

#### Modal

- 居中显示；遮罩 `rgba(0,0,0,.3)`；默认宽 760px、最大宽 `92vw`、圆角 8px；
- 结构为标题栏、内容区、底部操作区；标题 16px/600；
- 标题栏关闭按钮固定在右上角；
- 内容区一般使用 `padding:32px 24px`；
- 操作区按钮右对齐，按钮间距 12px；
- 仅在 PRD 明确时使用大型或全屏 Modal；移动端全屏时圆角 0。

#### Dialog

- 用于信息提示和二次确认，不承担复杂表单；
- 默认宽 600px、高 250px、圆角 8px；
- 类型包括 `info / success / warning / danger / confirm / system`；
- 删除确认使用 `confirm` 语义，确定按钮可使用 danger；
- Figma 每种类型至少一张状态实例，图标、标题、副标题和按钮文案使用真实中文。

#### Drawer

- 默认从右侧滑出，宽 800px，最大宽 100%；
- 标题栏高 56px，左右内边距 24px；
- 内容区 `page` 背景、可滚动；内部详情分区白底、8px 圆角、16px 内边距；
- 可选底部操作栏高 64px；
- 八列以上的维护型清单才使用 1200px 宽屏 Drawer；普通详情不得随意放大；
- 打开时锁定背景滚动、将焦点移入面板、支持关闭按钮/遮罩/Escape，关闭后恢复触发控件焦点；
- ModuleFrame 默认使用 iframe 内业务 Drawer；只有 PRD 明确要求跨越整个 App Shell 时，才通过受控 SystemFrame Portal 展示。

#### Popover

- 用于下拉、帮助说明、账号菜单、日期选择等短上下文操作；
- 白底、`line` 边框、8px 圆角、中层或轻阴影；
- 触发器和面板在 Figma 中作为一组状态展示；
- 面板打开时不能被父卡片或滚动容器裁切；
- 支持外部点击和 Escape 关闭；键盘焦点不能因鼠标离开规则丢失。

### 3.11 Toast

Toast 默认属于 SystemFrame 全局层；ModuleFrame 无法调用全局能力时才在 iframe 内按同一规格降级。

- 顶部 24px，水平居中；最小宽 320px，最大宽 `min(480px, 视口宽 - 32px)`，最小高 44px；
- 白底、8px 圆角、无描边、中层阴影；
- 内容内边距 10px 14px，图标与文字间距 10px；
- 使用 18px Lucide 语义图标：成功 `circle-check`、信息 `info`、警告 `triangle-alert`、错误 `circle-x`；
- 正文 13px、`ink-body`；同一条 Toast 只使用一种语义色；
- 默认展示 3000ms；连续触发更新原条目并重新计时；
- Figma 至少准备 `成功 / 信息 / 警告 / 错误` 四种实例和长文案换行实例；
- 不使用黑色实底胶囊作为默认 Toast；不把 Toast 做成需要用户决策的 Dialog。

### 3.12 空状态、加载态和错误态

Figma `客服入口、空状态` 以及组件页“提示”区域可作为状态参考。

每个列表型页面至少准备：

```text
正常有数据 / 空数据 / 加载中 / 加载失败 / 筛选无结果
```

- 空状态由图标、中文说明和可选操作组成；
- 加载态使用骨架或 loading 指示，不用静态“暂无数据”冒充加载；
- 加载失败提供可恢复操作，如“重新加载”；
- 筛选无结果应说明当前筛选无匹配，不与首次空数据混用；
- SystemFrame 的 iframe 加载失败和 ModuleFrame 的业务空状态必须在 Figma 中分开画，不得共用一张错误卡片。

## 4. 页面模板与布局

### 4.1 管理页标准骨架

所有后台列表/管理页面优先使用 `design.md` 的两区块结构：

```text
页面工作区
├─ 筛选区（可选状态 Tab + 流式筛选项 + 查询/重置）
└─ 数据区（标题行 + 操作 + 指标卡 + 表格 + 分页）
```

- 不设置独立的页面主标题卡片；页面标题整合到数据区标题行；
- 筛选区和数据区是两个白色内容区，不再在 App Shell 内套额外外框；
- 筛选项桌面基准宽 400px，最多 4 项/行，空间不足自然换行；
- 页面整体不使用 `max-width + 居中` 收窄，业务根保持占满 Router Outlet；
- 卡片内部使用 20px/24px 内边距，不把页面外层 16px 和卡片内边距混用。

### 4.2 Figma Auto Layout 与约束

HTML 页面转 Figma 时，优先使用以下布局映射：

| HTML/CSS 意图 | Figma 实现 |
| --- | --- |
| `display:flex; flex-direction:column` | 垂直 Auto Layout |
| `display:flex; align-items:center` | 水平 Auto Layout + 垂直居中 |
| `flex:1; width:100%` | `Fill container` |
| 固定按钮/图标尺寸 | `Fixed width/height` |
| 文案撑开按钮 | `Hug contents` |
| 表格列稳定宽度 | 列 Frame 固定或最小宽度 + 外层横向滚动 |
| `position:fixed` 遮罩/弹层 | 独立状态 Frame 或 Overlay，不塞入普通内容流 |
| `position:sticky` 操作列 | 表格状态稿中单独标记固定列，不用静态截图假装滚动行为 |
| `overflow-x:auto` | 画布上提供窄视口状态，并在注释中标明横向滚动边界 |

禁止把整张网页截图直接作为唯一 Figma 交付物。高保真截图可作为比对参考，但按钮、输入框、表格、弹窗和导航必须保留可编辑图层或使用组件实例。

### 4.3 视口稿

至少按项目实际支持范围制作：

| 稿件 | 目的 |
| --- | --- |
| 1920 桌面 | 对应 Figma `头部/浏览器1920`，验证完整外壳和宽表格 |
| 1336/1366 桌面 | 对应 Figma `头部/浏览器1336`，验证常用工作区密度 |
| 1280 或 1024 平板 | 验证侧栏、筛选流和表格滚动 |
| 768 | 验证侧栏折叠/抽屉和表单收缩 |
| 小于 768 | 仅在 PRD 要求时制作移动态；弹窗/抽屉按 `design.md` 全屏或满宽规则处理 |

每个视口都要检查：文字是否换行、控件是否被裁切、表格是否可滚动、弹层是否越界、按钮宽度是否因文案变化而跳动。

## 5. HTML 到 Figma 的同步流程

### 5.1 输入和准备

1. 从 `index.html?page=<key>` 打开真实原型，确认当前 SystemFrame、路由和模块内容；
2. 阅读 `AGENTS.md`、`design.md` 和目标模块 PRD；
3. 记录页面视口、默认状态、数据量和所有可触发的浮层；
4. 确认 Figma 页面中是否已有对应组件，优先复用组件实例；
5. 先建立页面 Frame 和外壳边界，再放入业务内容。

### 5.2 DOM 到 Figma 的映射

| HTML 原型 | Figma 交付 |
| --- | --- |
| 页面工作区 | 页面级 Frame，使用页面 Token |
| `filter-flow` | 水平/换行 Auto Layout，保留每个字段的独立标签和控件 |
| 数据区标题行 | 标题文本 + 右侧操作组的水平 Auto Layout |
| KPI 卡 | 复用指标卡组件实例，数字和趋势为可替换内容 |
| `table` | 表头、数据行、操作列均为可编辑结构；不要只放整张图片 |
| 分页器 | 复用 `表单/分页` 或新稿分页组件，提供页码状态变体 |
| Modal | 页面默认态 + 独立打开态 Frame，遮罩和焦点目标写入说明 |
| Drawer | 默认态 + 右侧打开态 Frame，标明 ModuleFrame/SystemFrame 所有权 |
| Toast | 触发前后两张状态稿，保留长文案和多条并发示例 |
| Tooltip/Popover | 触发器与浮层组合状态，注明 hover、focus 和外部点击关闭 |

### 5.3 组件复用规则

- 已有组件能表达时必须使用实例，不要 Detach；
- 需要新状态时先扩展组件变体，再在页面上局部绘制；
- 业务文案用实例属性替换，不通过复制组件并改名制造新组件；
- 新组件至少包含默认、悬停、禁用/错误中与响应式边界中的必要状态；
- 组件集中的属性名、顺序和默认值保持一致；
- 临时探索稿放入 `99 归档`，不要混入正式组件页。

### 5.4 Figma 页面说明格式

每张完整业务页面 Frame 建议附一个中文说明块，包含：

```text
页面：费用管理 / 订单管理
入口：index.html?page=费用管理
视口：1366 × 768
状态：默认有数据
壳层：SystemFrame（复用）
模块：ModuleFrame（订单列表）
交互状态：筛选展开、订单详情抽屉、导出成功 Toast
数据来源：Prototype/费用管理.html 的 mock data
例外：历史组件值 / PRD 特例 / 待确认项
```

说明文字是设计交付元数据，不要把它当成最终页面的可见标题或装饰英文文案。

### 5.5 交互状态落稿方法

每个页面至少建立以下状态 Frame 或 Prototype 连接：

1. 默认有数据；
2. 筛选/搜索后的结果；
3. 无结果或空数据；
4. 关键按钮悬停、禁用和加载；
5. 关键表单错误；
6. Dialog/Modal/Drawer 打开；
7. 操作成功 Toast；
8. 操作失败或服务异常；
9. 768px 边界布局。

原型中已有交互才能在 Figma Prototype 中连线；无法从 HTML 还原的后端异步状态必须用状态说明标记为“演示态”。

## 6. 费用管理页面落稿约定

费用管理是当前重点业务，页面稿应以 [`PRD/费用管理PRD.md`](./PRD/费用管理PRD.md) 为业务行为来源，以 `design.md` 为视觉来源。

### 6.1 订单管理

建议页面与状态命名：

```text
费用管理/订单管理/桌面/默认
费用管理/订单管理/桌面/筛选结果
费用管理/订单管理/桌面/空状态
费用管理/订单管理/桌面/订单详情抽屉
费用管理/订单管理/桌面/导出成功
费用管理/订单管理/桌面/导出失败
```

页面必须保持：

- 筛选区与数据区两区块结构；
- 创建时间使用连续的日期范围控件；
- 订单号、金额、时间使用 Mono；
- 状态列使用纯语义色文本；
- 订单详情默认使用 800px 右侧 Drawer；
- 导出反馈使用全局 Toast，业务页不重复创建 SystemFrame Toast；
- 表格横向滚动时不裁切操作列和最后一行边框。

### 6.2 开票管理

建议页面与状态命名：

```text
费用管理/开票管理/桌面/订单资格
费用管理/开票管理/桌面/开票记录
费用管理/开票管理/桌面/申请开票-第一步
费用管理/开票管理/桌面/申请开票-第二步
费用管理/开票管理/桌面/申请开票-第三步
费用管理/开票管理/桌面/常用抬头抽屉
费用管理/开票管理/桌面/申请详情抽屉
费用管理/开票管理/桌面/不可申请原因 Dialog
```

已确认的业务特例：

- 申请开票使用 760px 三步 Modal；
- 字段标签列 112px，控件列 400px，标签右对齐；
- 抬头类型、发票类型使用单选；抬头输入与下拉选项属于同一控件组合；
- 企业和个人模式的字段可见性、必填校验必须分别落稿；
- 常用抬头使用 1200px 右侧 Drawer；
- 申请详情使用 800px 右侧 Drawer；
- 不可申请原因使用 760px 居中 Dialog/Modal；
- 提交成功、资格变化、校验失败、下载失败都要有对应 Toast 或 Dialog 状态；
- 订单资格表和开票记录表都保留分页、横向滚动和末行边框。

## 7. 文案、图层与可交付性

### 7.1 中文界面文案

- 页面标题、区块标题、字段标签、表头、按钮、状态和空/错状态全部使用简体中文；
- 禁止装饰性英文眉题、英文副标题和中英并列字段；
- `ID`、订单号、接口参数、事件名、文件格式、版本号等技术值可以保留原文，并使用 Mono；
- Figma 组件示例中的 `TAB001`、`按钮`、`输入内容` 等占位词只允许出现在组件演示板，不得出现在业务页面稿；
- 不使用区块编号、英文翻译标题或“设计稿专用”文案污染最终页面。

### 7.2 图层命名

页面层级建议：

```text
页面名称
├─ 系统外壳（复用实例）
├─ 业务内容
│  ├─ 筛选区
│  ├─ 数据区
│  └─ 分页
├─ 业务浮层
└─ 设计说明（交付后可隐藏）
```

要求：

- Frame、组件、实例和说明层都使用有意义的中文名称；
- 不使用连续默认编号作为正式图层名；
- 交互说明放在独立说明层或 Prototype 说明中，不盖住业务内容；
- 组件实例保持可编辑文本和可替换图标；
- 不把多个独立交互控件合并成一张不可编辑图片。

### 7.3 不裁切验收

交付前逐个检查：

- Frame 四边没有截断阴影、圆角或边框；
- 表格最后一行边框完整；
- 长中文文案不会溢出按钮、标签、Badge 或弹窗；
- Popover、下拉、日期面板不会被父容器裁切；
- Drawer 顶部、底部和滚动区域完整；
- 遮罩覆盖范围与 SystemFrame/ModuleFrame 所有权一致；
- 1366、1280、768 三种宽度至少各检查一次；
- 导出 PNG/PDF 时没有把 Figma 右侧说明、选择框或画布背景带入成品；
- 没有“区块 1”“英文翻译标题”“TAB001”等不应出现在最终页面的内容。

## 8. 交付检查清单

### 8.1 来源与规范

- [ ] 已读取 `AGENTS.md`、`design.md` 和目标模块 PRD；
- [ ] 已确认 Figma 页面、组件页和历史组件值；
- [ ] 新稿颜色、字号、圆角、间距和阴影均绑定 `design.md`；
- [ ] 历史 Figma 值已单独标记，没有悄悄写入全局变量；
- [ ] 未确认的尺寸或行为标记为待确认。

### 8.2 组件与结构

- [ ] 优先复用现有 Figma 组件实例，没有无理由 Detach；
- [ ] 按钮、输入框、导航、表格、分页、Modal、Drawer、Toast 都有需要的状态；
- [ ] SystemFrame 与 ModuleFrame 边界清晰，没有重复外壳；
- [ ] 页面使用 Auto Layout、Fill/Hug/Fixed 约束，未用大量手工绝对定位；
- [ ] 图层名称清晰，组件属性可理解，业务文案可替换。

### 8.3 视觉与响应式

- [ ] 页面采用统一的筛选区/数据区骨架；
- [ ] 表头、数据行、末行边框、操作列和分页位置符合 `design.md`；
- [ ] 数字、订单号、金额、时间戳使用 Mono；
- [ ] 颜色语义正确，价格色没有被用于错误或警告；
- [ ] 长文案、空态、错误态、加载态均不会造成布局跳动或裁切；
- [ ] 1920、1336/1366、1280/1024、768 视口稿通过检查；
- [ ] 不出现英文标题、中英双标题、区块编号或组件占位文案。

### 8.4 交互与浮层

- [ ] Tab、筛选、搜索、日期、选择器的打开/选中/关闭状态完整；
- [ ] Modal、Dialog、Drawer 支持关闭按钮、遮罩和 Escape 的说明；
- [ ] Drawer 的宽度、焦点、滚动和所有权标注正确；
- [ ] Toast 至少覆盖成功、信息、警告和错误；
- [ ] 失败、空数据、无结果和权限不足状态可恢复或有明确下一步；
- [ ] Figma Prototype 连线与 HTML 原型的可见行为一致，异步演示态已说明。

## 9. 待确认项与维护规则

以下内容不应在只读核对阶段被当作最终事实：

- 组件页部分展示板的精确间距、内部图标线宽和所有变体属性；
- 历史组件的完整颜色样式库和变量绑定关系；
- 只读权限下无法展开的嵌套实例和局部 Auto Layout 约束；
- 业务页面中尚未落成组件的特殊插图或平台资源。

后续获得编辑权限或设计师提供最新组件库时，按以下顺序更新：

1. 先核对 Figma 变量和组件集；
2. 再更新本文件中的“已核实”表格；
3. 若视觉规范发生变化，先更新 `design.md`，再同步 HTML 和 Figma；
4. 若只是 Figma 组织或命名变化，更新本文件，不在 `design.md` 增加 Figma 管理细节；
5. 每次变更在 `00 说明与变更记录` 记录日期、影响页面和迁移范围。

---

**一句话原则：** HTML 原型负责证明真实交互，Figma 设计稿负责表达可复用结构；两者共享 `design.md` 的现行视觉 Token，并通过 SystemFrame/ModuleFrame 边界保持一致。
