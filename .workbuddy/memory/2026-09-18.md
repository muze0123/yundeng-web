# 2026-09-18 工作日志

## 开票管理分阶段交付：新增一期 PRD 与一期开发简要清单

- **需求**：费用管理-开票管理模块分阶段开发。一期只做开票订单、开票记录、常用抬头 + 用户申请开票（经第三方开票下发给用户），不做红冲相关业务；二期再做红冲任务与开票配置模块。要求输出《开票管理（一期）PRD.md》与一期客户端开发简要清单（面项目组通知）。
- **产出（新增，未改动 HTML）**：
  - `PRD/开票管理/开票管理（一期）PRD.md`：基于 03 全量 PRD 拆分重写的自洽一期契约。含 0.2 一期/二期范围决议表、0.3 一期依赖与不做的前提、一期主流程（申请→财务审核→第三方开票→站内/邮件交付）、5 段详情抽屉、7 类客户端状态、22 类原因码、一期操作矩阵、一期接口清单、12 条业务规则、24 条 AC、附录 B（与 03 的对应关系）。
  - `PRD/开票管理/一期客户端开发简要清单.md`：面项目组的范围通知（做什么/不做/状态矩阵/接口/联调/依赖/验收/待确认），105 行。
  - `00-开票业务文档索引.md`：阅读顺序表新增 3.1、3.2 两行指向一期文档。
- **一期口径关键决策**（需评审确认）：
  - 客户端状态集收敛为 7 类：待审核、已驳回、已撤回、已取消、开票中、开票失败、已开票；红冲三态（红冲处理中/红冲失败/已红冲）移二期。
  - 原因码 = 20 类基础 + 2 类派生（`INVOICED`、`ISSUANCE_FAILED`）；剔除 `RED_CONFIRMATION`/`RED_PROCESSING`/`RED_FAILED`/`REPLACEMENT_PENDING` 与「已红冲资格分流」表。
  - 详情抽屉一期 5 段（无「红冲与重开」）；底栏「已开票」去掉「更正」；记录状态筛选与分组排序去掉红冲态。
  - 过渡约定：已开票后退款一期不生成红冲任务、不改已开票记录，金额差异走财务线下台账（记为 O-09）。
  - 「开票配置模块」一期不产品化，配置由财务后台/人工维护；模块归属记为待确认 O-08。
  - 数据模型一期不写红票，但预留 `document_type`/`source_document_id`/`red_reason` 字段降低二期迁移成本。
  - 一期无批量选择，移除 `max_selected_orders` 配置。
- **备注**：03 全量 PRD 的 L45/L876 表格单元格内 `invoiceTab=orders|records|titles` 含未转义管道符会破坏表格渲染（历史遗留，本次未改 03）；新一期 PRD 已避免该写法。

## 开票订单工具栏：新增导出/刷新按钮

- **需求**：开票订单页签在「当前可开票金额」文本左侧新增导出、刷新按钮。
- **改动**（均在 `Prototype/费用管理-开票管理.html`）：
  - `upgradeInvoiceListLayout()`：订单工具栏 `#invoiceOrderToolbarActions` 内按「导出→刷新→金额指标」顺序插入 `#invoiceOrderExport`、`#invoiceOrderRefresh`（与记录页签导出/刷新同款 `btn btn-default` 样式，DOM 顺序一致）。
  - 事件绑定：导出→`openModal('导出开票订单', …)` 按 `invoiceOrderRowsCache.length`（≤500）提示；刷新→`renderInvoiceOrders()+renderInvoiceRecords()+toast('开票订单已刷新')`。
  - CSS：`.invoice-toolbar-metrics` 加 `margin-left:8px`；`@media(min-width:901px)` 中订单搜索组 flex-basis 由 `480px` 改 `clamp(300px,23vw,440px)`，并对 `.invoice-list-toolbar .order-toolbar-actions` 加 `flex-shrink:0`——否则 1280–1440 宽度下金额指标会因差约 9px 被挤到第二行。
- **验证**（agent-browser + 本地 8899，双层 iframe 逐层探查）：1280/1366/1440/1920 四档宽度按钮与三组金额均同行；导出弹窗标题/条数正确；刷新 toast 正常；记录页签导出/刷新不受影响；控制台无报错。注意改 CSS 后需 cache-bust 强刷才生效。

## 开票管理独立版导出（exports/开票管理-独立版.html）

- **需求**：把开票管理模块封装为可分享给技术单独打开的单文件 HTML，放 `exports/`。
- **产物**：`exports/开票管理-独立版.html`（约 408KB），由 `Prototype/费用管理-开票管理.html` 转换生成，构建脚本在 `/tmp/build_invoice_standalone.py`（临时，未入项目）。
- **转换要点**：
  1. 内联 `筛选布局.css`、`公共导航.css`、`分页器.css`、`src/styles/global.css`、`分页器.js`（YDPager）；不再引用 `公共导航.js`；
  2. 移除「直接打开回跳 index.html?page=billing-invoice」脚本；静态加 `body[data-yundeng-embedded="true"]` + `#mainContent[data-yundeng-business-root][data-yundeng-embed-path]` 复现嵌入态布局（16px 内边距、占满视口）；
  3. 保留 Tailwind/Lucide/字体 CDN（页面自带 Tailwind 失效回退样式）；文件头加出处与差异说明注释；标题改为「云登 · 开票管理（独立版）」；
  4. 保留云币/订单/优惠券隐藏面板（其 DOM 被共享初始化脚本引用，删除会崩）。
- **关键坑**：`分页器.js` 的 JSDoc 注释里含 `</script>` 字样，直接内联会让解析器提前终止脚本块导致整页空白——内联 JS 前必须把 `</script` 转义为 `<\/script`。
- **验证**：file:// 直开 + agent-browser 冒烟全过——订单 10 行/36 条分页 4 页、申请开票三步弹窗、筛选抽屉、导出弹窗、刷新 toast、记录 12 列详情抽屉、常用抬头 6 行 12 列及新增抬头编辑弹窗；网络请求全部 200，仅 Tailwind CDN 常规警告。
