# 2026-09-16 工作日志

## 商城-购物车：结算区三卡片合并
- 需求：将右侧结算栏的「订单信息 / 支付方式 / 应付金额」三个独立卡片合并为同一张卡片，用分割线区分。
- 实现（仅改 `Prototype/商城-购物车.html`）：
  - `renderSummary()`：三个 `<section>`（summary-card / summary-payment / summary-total-card）合并为一个 `<section class="summary-card">`，内部为三个 `.summary-block`（summary-order / summary-payment / summary-total），JS 提取 `selectedList`、`paymentOptions` 两个局部变量提升可读性。
  - CSS：卡片样式只保留在 `.summary-card` 上（改为 flex column）；新增 `.summary-block + .summary-block { margin-top:16px; padding-top:16px; border-top:1px solid var(--line-light) }` 作为区块分割线；删除旧的三卡片共享边框规则。
  - 底线对齐：`.order-summary > section` / `> .summary-total-card` 的 flex 规则替换为 `.order-summary > .summary-card { flex:1 1 auto; min-height:0 }`，保持右栏与左侧列表底对齐、留白仍在卡片底部。
  - 保留类名 `summary-payment` 使 `.summary-payment h3` 等后代选择器继续生效。
- 验证：node --check 内联脚本通过；用临时 iframe harness（1680px 宽）+ agent-browser 截图确认三区块同卡片、分割线正常、无重复壳层/裁切。

## 商城-云币充值：左侧区块补圆角

- 需求：`Prototype/商城-云币充值.html` 左侧白色业务区块未按 design.md 规范加圆角。
- 依据：design.md 3.1「8px 用于卡片、弹窗、大容器」、5.10 卡片分层规则；右侧 `.coin-order-summary` 已是 8px。
- 实现（仅改该 HTML）：`.coin-form-column` 增加 `border-radius:8px`（原先无圆角）；`.coin-business-card` 后置覆盖规则由 `border-radius:0` 改回 `8px`，保持与外层同层级。
- 验证：agent-browser 打开 `index.html?page=store-coin`（Esc 关闭新手引导弹层后）截图放大确认左栏四角均为 8px 圆角，与订单详情卡一致，无裁切。
- 经验：该页面顶部新手引导弹层会遮挡首屏，headless 验证时先按 Escape 关闭；`set viewport` 后偶尔出现标签页重置为 about:blank，重开即可。

## 商城-云币充值：订单详情卡去掉支付按钮上方分割线

- 需求：订单详情 div 中「立即支付」按钮上方的分割线去掉。
- 实现（仅改该 HTML）：删除 `.coin-order-payable-section` 的 `border-bottom:1px solid #F0F1F3`，保留原内边距（8/20/18px）避免布局跳动；标题下、明细列表下的分割线按原样保留。
- 验证：agent-browser 打开页面（Esc 关引导）截图放大确认按钮上方已无分割线，留白正常。
- 追加（用户反馈）：去线后未同步调间距，导致 44px 空洞（payable-section 18px 下内边距 + actions 8px 外边距 + 18px 上内边距叠加）。已改为：`.coin-order-payable-section{padding:8px 20px 0}` + `.coin-order-actions{margin-top:18px;padding:0 20px 18px}`，间距统一 18px，与卡片内部节奏一致。
- 教训：删除分隔元素（分割线/卡片合并）时，必须同时检查相邻两段 padding/margin 叠加值，主动收敛到设计梯度，不要等用户指出。



## 编辑浏览器：指纹设置新增高级配置提示条

- 需求：指纹设置区块顶部加警示文案「建议对浏览器环境有较深入了解的技术人员修改高级配置…部分参数要求客户端版本：V1.4.1.0及以上版本」，其中「对浏览器环境有较深入了解的技术人员修改高级配置」和「V1.4.1.0」高亮（参考用户截图，截图内是 V6.21.2，按用户文字用 V1.4.1.0）。
- 实现：`#section-fingerprint` 顶部插入 `.fp-advanced-tip` 提示条（warning-bg #FDF2E9 底 + 橙色 ! 圆标 + `.tip-hl` 橙色 #E7772D 高亮，色值取自 design.md 语义色 warning）。浏览器验证：展开指纹区后提示条渲染正常、两处高亮生效。
## 商城-代理：类型 tab、时长单选与资源卡片宽度调整
- 最终需求（经一次修正）：代理类型 tab（`.proxy-type-tabs .segment-button`）宽度固定 **100px**；购买时长单选框（`.duration-options .segment-button`）宽度固定 **100px**；下方资源产品卡片（`.proxy-type-card .resource-grid` 内 `.resource-card`）宽度固定 **280px**。
- 实现（仅改 `Prototype/商城-代理.html`，在 `.segment-button .discount-tag` 规则后新增三条容器限定规则）：
  - `.proxy-type-tabs .segment-button { width:100px; min-width:100px; flex:0 0 100px; padding:0 8px }`
  - `.duration-options .segment-button { width:100px; min-width:100px; flex:0 0 100px; padding:0 8px }`
  - `.proxy-type-card .resource-grid { grid-template-columns: repeat(auto-fill, 280px) }`
- 关键点：基础 `.segment-button`（含"参考购买页"块 min-width:280px）与各处 `.resource-grid` 媒体查询（repeat(4/2,1fr)、1fr）特异性均为 (0,1,0)，通过容器选择器 (0,2,0) 覆盖即生效，且与声明顺序无关。
- 验证：`python3 -m http.server` 起本地服务使嵌套 iframe 同源，经 index → 系统框架.html → 商城-代理.html 三层 contentDocument 实测：tab [100,100,100]、时长 [100,100,100,100]、资源卡片 10 张全为 280px、gridTemplateColumns = "280px 280px"；截图确认无裁切溢出。
- 路由备忘：商城代理规范 URL 为 `index.html?page=store&moduleSearch=tab%3Dproxy`（`tab=proxy` 是 iframe 内模块参数，需经 moduleSearch 传递；直接 `?page=store&tab=proxy` 会被路由到 store-coin）。首次加载会弹新手引导，先按 Esc 关闭再截图。
## 商城-代理：优惠券下拉框选中态样式修复
- 问题：`#proxyCoupon`（原生 `<select class="control">`）使用浏览器原生外观——原生粗箭头、占位项与选中项同为正文色，与 design.md 的 `.pg-select` 规范不一致，选中后视觉不协调。
- 修复（仅改 `Prototype/商城-代理.html`）：
  - `select.control` 增加 `appearance:none/-webkit-appearance:none`，改用 design.md 规定的自定义箭头（内联 SVG data URI 描边 `#9DA2AC`，`background-position: right 10px center`）；补充 `:hover` 主色描边。
  - `#proxyCoupon` 宽度规则上移到 `.control` 区块（去重，原「参考购买页」块内同名规则删除）；新增 `#proxyCoupon:has(option[value=""]:checked) { color: var(--ink-muted) }`，未选择时占位灰、选中后恢复正文色。
- 验证（本地服务 + 三层 iframe eval）：computed `appearance: none`、背景箭头已加载、占位色 `rgb(157,162,172)`、选中色 `rgb(58,63,74)`、宽度 326px；4 倍放大截图确认箭头与文字对齐正常。
- 经验：本页无自定义下拉组件，统一用原生 select + `appearance:none` + 背景箭头，符合 design.md「下拉菜单组件」约定。
## 商城-代理/套餐/云币充值：优惠券下拉框「两条蓝边」修复（跨三页统一）
- 现象：优惠券下拉框选中（=聚焦）后出现两条蓝色边线。根因：焦点态叠加了三层视觉——`1px` 主色 border + `box-shadow:0 0 0 2px rgba(0,102,255,.12)` 阴影环 + 全局 `:where(button,input,select):focus-visible{outline:2px solid var(--primary);outline-offset:2px}`（代理页 line≈419，与 `.control{outline:none}` 同特异性但声明更靠后故胜出）。
- 统一修复口径：优惠券下拉框焦点态**只保留单条主色描边**（`outline:none; box-shadow:none; border-color:主色`），并统一 `appearance:none` + design.md 自定义箭头（SVG data URI，`#9DA2AC`，`right 10px center`）与占位灰。
- 三页改动（均只改 CSS，套餐页另加一个 option 的 value=""）：
  - `Prototype/商城-代理.html`：`#proxyCoupon:focus/:focus-visible` 单描边。
  - `Prototype/商城-套餐.html`：`.select-control`(#planCoupon) 与 `.recharge-select`(充值弹窗 #rechargeCoupon) 合并统一样式；弹窗占位 option 补 `value=""` 以便用 `:has(option[value=""]:checked)` 做占位灰（该页 token 用 `--muted`）。
  - `Prototype/商城-云币充值.html`：`.coin-recharge-select` 同款箭头；`#rechargeCoupon:focus` 覆盖共享 `.field:focus` 的阴影环；占位灰用 `option[value="0"]:checked`。
- 验证方法（可复用）：嵌套 SystemFrame 的 file:// iframe 跨源无法 eval，且 http 下浏览器守护进程偶发丢页；改用**临时 iframe harness**（`exports/_tmp-coupon-harness.html`，同源 http，`?module=X.html&sub=tab%3D…` 参数化）在单层 iframe 内 eval 计算样式 + 截图，验证后删除。三页实测均为 `border: rgb(0,102,255)` / `box-shadow: none` / `outline: none`。
- 文案修订：按用户新文案整句替换为「建议对浏览器环境有较深入了解的技术人员修改指纹设置，若参数值设置异常，会影响该环境中账号的正常使用！」——去掉「高级配置」措辞和「部分参数要求客户端版本 V1.4.1.0」整句；高亮改为「对浏览器环境有较深入了解的技术人员修改指纹设置」。浏览器验证通过。

## 商城-套餐：订单详情面板去掉订阅套餐按钮卡的阴影
- 需求：订单详情面板内「订阅套餐」按钮所在的 `.total-card`（嵌套 `.panel`）自带 box-shadow，在面板底部形成"小卡片"阴影边界，要求去掉。
- 实现（仅改 `Prototype/商城-套餐.html`）：`.total-card` 后置覆盖规则追加 `box-shadow:none; border-radius:0;`，按钮区融入父卡片，不再出现独立小卡片阴影。
- 验证：iframe harness（`?embedded=1` 绕过直开回框架）+ Chrome headless 截图，对比修改前后右栏裁剪图，阴影消失、按钮位置与面板底边正常。
- 经验：headless 截图该原型的方法——先用 harness 页面在 file:// 下 `localStorage.setItem('yundeng-new-user-guide-auto-consumed-v1','1')` 再跳转目标页可跳过新手引导弹层；`--user-data-dir` 方案在本环境会挂起不可用。
## 编辑浏览器：换绑代理弹窗 5 项改动

1. 本地网络：IP 查询渠道右侧新增【检查网络】按钮（proxyNetworkBtn），按钮内 loading→结果胶囊（成功「检测成功 IP:36.27.25.50 地区：中国杭州」/失败「检测失败」）；原型用 proxyNetworkTick 交替返回两种结果，如需固定成功改 `%2===0` 判断即可。仅本地网络显示。
2. 弹窗字段标签补回全角冒号：normalizeFieldLabels 会把 .field-label 文本里的'：'全部删掉（44 个标签 0 冒号的页面级现状），弹窗内用 `#proxyEditor .proxy-dialog-field .field-label:after{content:'：'}` 恢复（代理类型+IP 查询渠道两个标签都补了）。坑：弹窗 HTML 在 #browserForm 内，safe 徽标规则 `#browserForm .field-label[data-semantic=safe]:after` 与冒号规则特异性打平且提供 background，需在冒号规则里重置 background/border/font。
3-5. 平台/自有/API：「全部状态」→「是否绑定环境」；刷新按钮改纯图标（仅 aria-label+title），toolbar 末列 justify-self:end 固定列表右上角；自有代理新增「是否显示过期代理」下拉（不含/包含过期代理，默认不含，isProxyExpired 按 expires 与当前时间比较真实过滤，mock 数据默认滤掉 2 条）。toolbar 网格改 6 列、搜索框改 minmax(180px,1fr) 自适应防溢出。
- 工具坑：agent-browser 里 scrollIntoView 会让 fixed 弹窗内部矩形测量错位（假象），全新加载后恢复正常；视口设置命令是 `agent-browser set viewport <w> <h>`。
## 编辑浏览器：换绑代理弹窗第二轮（检查网络扩展 + ？气泡 + 随机选择）

- IP 查询渠道 + 检查网络 + ？气泡扩展到 本地网络/自有代理/代理API 三类（platform 仍隐藏）；？用既有 data-help 弹层体系，新增 key `proxy-ip-channel`（按用户原文），触发器 `#proxyIpHelpBtn`（field-help-trigger field-help-icon + circle-help）。
- 修复既有 bug：`#proxyHelpPopover` 原 z-50，被 z-[1100]/[1150]/[1200] 弹窗压住（添加账号弹窗里的 data-help="ip" 气泡其实一直是坏的），改 z-[1300] 后正常；closeProxy/setProxyType 里补 hideFieldHelp() 防残留。
- 自有代理新增 `#proxyRandomPick` 复选框（proxy-list-actions 内、按钮组前）：勾选→隐藏 #proxyListActionButtons 与 #proxyListShell，显示 #proxyRandomHint（虚线框提示池数量）；确定→从 proxyRandomPool（owned + environmentCount===0 + 未过期）随机取一条 applyProxySelection；renderProxySelection 与 confirmProxy.disabled 同步适配 randomOn。openProxyEditor 重置勾选。
- CSS 坑照旧：ID 级特异性需显式 `.hidden` 覆盖（proxy-list-shell/proxy-list-action-buttons/proxy-random-hint/proxyIpHelpBtn）；移动端 media query 给 hint 补 grid-column:1;grid-row:3。
- 工具坑：agent-browser 会话间歇性被重置回 about:blank（截图/等待期间），需 close+open 重开；eval 里取弹层必须用模块 iframe 的 document（d.getElementById），顶层 document 拿不到。

## 侧边栏：同组二级菜单切换闪动修复（软导航）
- 根因：菜单二级切换走 `location.assign` 整壳重载 `系统框架.html`，侧栏 DOM 重建 + 模块 iframe 白屏，视觉上表现为一级菜单"收起-展开"闪一下。实测展开状态数据本身正确（无状态翻转）。
- 实现（仅改 `Prototype/公共导航.js`，四处）：
  1. `embeddedModuleSrc()` 泛化为 `buildEmbeddedModuleSrc(moduleFile, shellSearch)` 供软导航复用；
  2. `setupBrowserFrame` 暴露 `window.yundengSyncBrowserRoute(title, address)` 同步伪浏览器标签与地址；
  3. `setupRouter` 新增 `parseShellRoute / applySidebarActiveState / applySidebarExpandedState / softApplyRoute`：软导航 = pushState（index 宿主模式跳过、由外层 pushState）+ 更新侧栏激活/展开态 + 换模块 iframe src + 同步标题；`loadTimeout` 改 let、新增 `activeRoutedModule/activePageLabel`（module-ready 与 load-failure 判断改用，避免软导航后被误判回硬导航）；非 index 宿主注册 popstate 软恢复；`navigate()` 优先 softApplyRoute，失败回退原 assign 路径；replace 流程保持硬导航；
  4. 菜单链接点击（`.yundeng-menu-link/.yundeng-create/.yundeng-bottom-link`）改经 `yundengNavigateShell` 软导航并 preventDefault。
- 缓存参数：全站 30 处 `公共导航.js?v=*` 统一升级为 `v=20260916a`（否则浏览器拿旧脚本）；注意 公共导航.css 参数仍不统一（20260825a/20260828e 两种，本次未动 CSS 未改）。
- 验证：同组（订单管理→云币交易流水）壳文档存活（marker 法）、侧栏 DOM 不重建、模块 iframe src/标题/外层 URL 同步；跨组（→团队管理）原组收起新组展开、模块渲染 56KB 正常；手动分组收起/展开正常；后退跨条目整页恢复与旧版一致（无回归）。

## 开票管理：常用抬头搜索框占位符改文案
- 需求：常用抬头页签搜索框提示语改为「请输入关键词，多个用中文逗号分隔」（原为「请输入抬头名称、税号或邮箱」）。
- 实现（仅改 `Prototype/费用管理-开票管理.html` L739 工具栏 innerHTML 模板字符串）：`#invoiceTitleKeyword` 的 placeholder 替换；该文案与开票订单页 `#orderGlobalSearch` 占位符口径一致。
- 备注：搜索逻辑本身为 OR 组合多关键词（已有实现），仅改提示语；PRD 文档未引用旧占位符文案，无需同步。
