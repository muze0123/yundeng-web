# 编辑浏览器产品需求文档

> 交付对象：智能编码工具 / 产品设计 / 前端 / 后端 / 测试
> 产品：YunLogin PC 端
> 页面：编辑浏览器
> 实测版本：v4.0.2.4
> 文档日期：2026-08-10
> 原型同步基线：`Prototype/编辑浏览器.html`（2026-08-10 交互版）
> 状态：可进入原型与开发阶段

## 0. 文档依据与实现指令

### 0.1 依据与优先级

本 PRD 基于以下材料生成：

1. YunLogin 已登录客户端中“编辑浏览器”页面的实际操作与状态回放；
2. 项目内 `design.md` 视觉规范；
3. 项目内 `claude.md` 原型工程规范；
4. 项目内《系统框架PRD.md》；
5. 《Vibe Coding产品PRD模版》以及《角色管理PRD》《成员管理PRD》的页面布局与模块布局写法。

出现冲突时，优先级为：实测业务行为 > 本 PRD 的明确验收规则 > `design.md` > `claude.md` > 推测性建议。

### 0.2 智能编码执行指令

生成可双击预览的高保真单文件 `编辑浏览器.html`，使用语义化 HTML5、Tailwind CSS CDN、Lucide 图标和原生 JavaScript。首版使用内联 Mock 数据，不依赖真实后端，不引入 React、Vue、jQuery。所有输入框、下拉框、分段按钮、复选框、表格、折叠区、提示、分页、保存和取消必须真实可交互，浏览器控制台无错误。

必须复用系统框架的侧栏与顶栏。页面采用“顶部主配置 + 左侧分区表单 + 右侧粘性指纹摘要 + 底部固定操作栏”结构，不得改造成卡片瀑布流或营销页。敏感字段（密码、2FA、Cookie、代理提取链接）不得出现在日志、Toast、URL 或埋点属性中。

## 1. 问题陈述

用户在一个浏览器环境中同时维护出口网络、账号资产、浏览器内核、操作系统、UA、启动数据、指纹伪装、插件与运行偏好。配置项数量多、风险不同且存在强联动：代理 IP 与语言、时区、地理位置需要匹配；内核、操作系统与 UA 需要一致；WebGL、硬件和设备标识需要形成可信设备画像；账号密码、Cookie 与 2FA 又属于敏感资产。

当前实测页面具备完整配置能力，但部分操作缺少防误触保护：随机生成指纹会立即覆盖值，取消会直接丢弃修改；名称超过限制采用“允许输入、显示错误”的策略。若实现时忽略这些状态与联动，容易造成代理不可用、账号风控、指纹自相矛盾、未保存数据丢失或敏感信息泄露。

## 2. 解决方案

提供一个可编辑、可校验、可预览、可回退的浏览器环境配置页：

- 顶部完成名称、代理、网络检测与账号绑定；
- 页面进入时由系统默认生成一套完整指纹并展示最终生效摘要；
- “自定义指纹配置”下按基础设置、指纹设置、插件选择、偏好设置四个折叠区组织；
- 右侧实时展示最终有效指纹，而不是仅展示原始输入；
- 所有配置写入统一草稿态，点击“保存修改”才持久化；点击“取消”按 dirty 状态执行离开保护；
- 使用安全项、风险项、必填项三类视觉标识；
- 对代理、IP、OS、内核、UA、硬件及设备标识执行一致性校验；
- 对账号密码、2FA、Cookie 等敏感信息实行最小展示、最小传输、禁止日志记录。

## 3. 用户、权限与成功指标

### 3.1 用户角色

- 团队成员：编辑其有权限的浏览器环境；
- 团队管理员：编辑团队环境、绑定代理和账号、覆盖团队偏好；
- 只读成员：仅查看配置与指纹摘要，所有修改入口禁用；
- 系统：完成网络检测、指纹生成、一致性校验与持久化。

### 3.2 成功指标

- 编辑保存成功率 ≥ 99%；
- 网络检测结果可理解率 ≥ 95%；
- 表单校验错误均能定位到具体字段；
- 代理或 OS 修改后，相关指纹摘要同步更新率 100%；
- 重复提交率 < 0.1%；
- 敏感字段进入日志或埋点的事件数为 0；
- 关键流程键盘可达率 100%。

## 4. 页面整体布局与模块结构

### 4.1 页面骨架

页面复用云登系统框架，业务页本身不重复创建侧栏、顶栏或全局账号菜单。页面空间关系如下：

```text
┌──────────────┬──────────────────────────────────────────────────────────┐
│              │ 顶部导航栏（高 56px，固定在应用内容区顶部）              │
│ 左侧导航栏   ├──────────────────────────────────────────────────────────┤
│ 展开 220px   │ 页面主滚动区                                             │
│ 收起 68px    │ ├─ 页面状态图例                                          │
│ 独立滚动     │ ├─ 左侧配置区 + 右侧指纹摘要                             │
│              │ └─ 页面末尾安全留白                                      │
│              ├──────────────────────────────────────────────────────────┤
│              │ 底部操作栏（固定，取消 / 保存修改）                      │
└──────────────┴──────────────────────────────────────────────────────────┘
```

- 顶部导航栏高 56px，左侧展示“环境管理 / 编辑浏览器”和未保存状态，右侧复用帮助、通知、语言及账号入口；导航栏不随主内容滚动。
- 左侧导航栏展开宽 220px、收起宽 68px，高度占满视口；菜单超长时仅侧栏内部滚动。
- 页面主滚动区是业务正文唯一的纵向滚动容器；弹窗打开后由弹窗内容区接管滚动，背景页面不得继续滚动。
- 底部操作栏固定在应用内容区底部，使用向上阴影区分层级；正文底部必须预留不小于操作栏高度的空间，最后一个字段不得被遮挡。
- 交互标注默认隐藏，只显示右侧“显示标注”浮标；用户点击后才渲染当前业务区及底部操作栏的标注。

### 4.2 主工作区布局

桌面端采用“左侧配置、右侧摘要”的双栏工作区：

```text
┌────────────────────────────────────────────────────────┬──────────────┐
│ 环境信息                                               │ 指纹信息摘要 │
│ ├─ 名称                                                │ 固定宽 292px │
│ ├─ 代理信息                                            │ 视口内粘滞   │
│ ├─ 网络检测                                            │ 实时更新     │
│ └─ 账号平台                                            │              │
├────────────────────────────────────────────────────────┤              │
│ 自定义指纹配置                         [随机生成新指纹] │              │
├────────────────────────────────────────────────────────┤              │
│ 基础设置（可折叠）                                     │              │
│ 指纹设置（可折叠）                                     │              │
│ 插件选择（可折叠）                                     │              │
│ 偏好设置（可折叠）                                     │              │
└────────────────────────────────────────────────────────┴──────────────┘
```

- 工作区列定义为 `minmax(0, 1fr) + 292px`，列间距 16px；左栏承载可编辑草稿，右栏只展示最终生效值。
- 环境信息区位于左栏首位，名称、代理、网络与账号都属于环境业务信息，不参与随机指纹覆盖。
- “自定义指纹配置”命令区位于环境信息区下方，保留 1px 浅蓝描边；其余主要业务区块使用底色与留白分层，不增加装饰性外描边。
- 四个可折叠配置区的白色标题区 hover 使用设计系统 `block-hover`（`#F0F1F3`），与页面底色 `bg-page`（`#F7F8FA`）保持区分；表格行和按钮继续使用各自 hover token。
- 基础设置、指纹设置、插件选择、偏好设置按阅读顺序纵向排列，允许多个区块同时展开。
- 指纹信息摘要在宽屏下相对主滚动区顶部粘滞；摘要内容过长时面板内部滚动，不推动底部操作栏。

### 4.3 页面模块布局

| 模块 | 位置与层级 | 内部组成 | 尺寸与间距 | 滚动、固定与联动 |
|---|---|---|---|---|
| 页面状态图例 | 主滚动区顶部 | 安全项、风险项、必填项说明 | 与工作区间距 16px；图标与字段标识一致 | 随正文滚动；不产生草稿变更 |
| 环境信息区 | 左栏首个业务模块 | 名称、代理摘要、网络检测、账号平台 | 标签列 120px；控件区 500px；字段间距 16px | 随正文滚动；换绑代理与账号操作打开弹窗 |
| 自定义指纹命令区 | 环境信息区下方 | 区块标题、说明、已自定义状态、随机生成按钮 | 左右分布；按钮不挤压标题 | 随正文滚动；生成后更新四个配置区和右侧摘要 |
| 四个配置区 | 左栏命令区下方 | 基础设置、指纹设置、插件选择、偏好设置 | 区块间距 12px；字段间距 16px | 折叠内容参与文档流；开合后重算标注位置 |
| 指纹信息摘要 | 工作区右栏 | 最终内核、系统、网络、设备及指纹值 | 固定宽 292px；长值最多两行截断 | 宽屏粘滞；低于 1180px 进入普通文档流 |
| 底部操作栏 | 应用内容区底部 | 取消、保存修改 | 高 64px；按钮右对齐，间距 12px | 固定；保存中禁用重复提交 |
| 业务弹窗层 | 页面上方全局层 | 换绑代理、添加代理、添加/编辑账号、选择已有账号 | 遮罩覆盖业务内容；标题栏和底栏固定 | 内容区独立滚动；关闭后焦点回到触发按钮 |

### 4.4 环境信息区局部布局

| 字段/子模块 | 页面布局 | 关键约束 |
|---|---|---|
| 名称 | 单行“标签 + 输入框 + 字数” | 输入框占 500px 控件区，字数位于输入框右侧；错误文案在控件下方 |
| 代理信息 | 第一行显示 500px 代理摘要，其后依次为“换绑代理”“自动分配代理”“什么是代理？”；第二行显示 IP 联动复选框 | 操作按钮和帮助入口不计入 500px 控件宽度；空间不足时整组自然换行 |
| IP 查询渠道 | 下拉框、检查网络按钮、问号帮助、检测结果同行 | 下拉框 500px；检测结果不得挤压或覆盖前置控件，空间不足时换到下一行 |
| 账号平台入口 | “添加新账号”“选择已有账号”双按钮组，帮助文案紧邻“选择已有账号”右侧 | 双按钮组宽 500px；帮助文案保持原位置，不放入任一账号弹窗 |
| 已绑定账号列表 | 账号入口下方 | 宽度不超过 920px；列为平台、名称、账号、操作；添加/选择完成后显示 |

### 4.5 响应式与溢出规则

- 宽度不低于 1180px：保持双栏，右侧摘要粘滞显示。
- 宽度低于 1180px：工作区改为单栏，摘要进入配置区之后的正常文档流；摘要列表限制最大高度并允许内部滚动。
- 宽度低于 768px：字段标签移到控件上方；500px、400px 固定控件改为 `100%`；底部按钮允许等宽排列。
- 表格超宽时只允许表格视口横向滚动，不得产生页面级横向滚动；表头保持粘滞。
- 长标题、帮助文案、状态摘要和按钮组不得互相覆盖；无法同行时按模块整体换行，不缩小到不可读尺寸。

## 5. 页面模块与功能规则

### 5.1 环境信息区

| 功能 | 实测行为 | 实现要求 |
|---|---|---|
| 名称 | 默认显示已有名称和 `7 / 100`；清空为 `0 / 100`；输入 101 字显示 `101 / 100` 和错误图标，输入本身未被截断 | 必填；trim 后不能为空；最多 100 字；采用允许输入并显示错误的策略；错误时不可提交 |
| 风险图例 | 绿色安全项、红色风险项、红色星号必填项 | 安全、风险图标采用与字段旁一致的 16px 描边圆形样式；提供文字和读屏文本，不能只用颜色表达 |
| 代理摘要 | 默认“本机网络”；有“换绑代理”和“自动分配代理” | 当前代理摘要必须包含类型、可脱敏 IP、地区与检测状态；“什么是代理？”使用灰色虚线下划线，悬停/聚焦展示平台代理、自有代理、代理 API、本地网络说明及蓝色“帮助中心”链接 |
| 修改 IP 自动重新生成指纹项 | 默认未勾选 | 仅代理发生变化且绑定成功后触发；刷新 IP 相关指纹并更新摘要；文案右侧不显示风险感叹号 |
| IP 查询渠道 | 默认 IP2Location | 下拉选项由后端枚举；切换渠道清除旧检测结果；低对比度灰色 `?` 位于“检查网络”按钮右侧，悬停/聚焦解释渠道用途与结果差异 |
| 检查网络 | 实测成功后展示 IP 与“中国杭州”地区 | loading → 成功/失败；成功显示出口 IP、国家/地区、渠道、延迟；失败保留配置并给出原因 |
| 账号平台 | 页面提供“添加新账号”和“选择已有账号”两个弹窗入口 | 两个入口同行显示；“账号平台有什么用？”紧邻“选择已有账号”右侧；弹窗间切换时先关闭当前弹窗；已绑定账号在入口下方列表展示，支持修改和解绑 |

#### 5.1.1 帮助气泡正式文案

**IP 查询渠道 `?`：**

1. 设置 IP 查询渠道后，将使用您选择的 IP 查询渠道对 IP 相关信息进行查询并在环境检测页展示。
2. 不同的 IP 查询渠道可能存在一定的检测结果差异；若代理检测结果与实际信息不符，可调整 IP 查询渠道以确保检测结果准确。

**什么是代理？**

- 【平台代理】：云登自营纯净 IP，团队统一购买、统一管理，即用即配，最适合店铺长期安全运营。
- 【自有代理】：用户从第三方获取的代理资源，在客户端上传配置即可使用，自主管控网络访问身份。
- 【代理 API】：通过第三方代理 API 自动提取 IP，无需手动配置，适合 IP 需求量大、动态场景使用。
- 【本地网络】：不使用代理，直接用本地 IP 访问。不推荐，多店铺共用同一 IP 易导致账号关联。
- 末尾显示“如需了解更多请查看 帮助中心”，其中“帮助中心”为蓝色可点击链接。

**账号平台有什么用？**

- 【什么是账号平台】：账号平台即用户要登录的网站或应用后台，如电商、社交、广告投放、企业系统等。
- 【选择账号平台有什么用】：选好对应平台后，可一键打开网站并自动登录，无需手动输入网址、账号和密码。
- 【如何使用账号能力】：① 选择账号平台，云登内置 400+ 平台，无对应平台可通过自定义网址添加；② 输入账号、密码，按需配置 2FA；③ 启动环境后，在环境检测页点击已配置的平台即可一键打开并自动登录。
- 末尾显示“如需了解更多请查看 帮助中心”，其中“帮助中心”为蓝色可点击链接。

所有帮助气泡均在鼠标悬停与键盘聚焦时可见；指针移入气泡后保持打开，移出触发器与气泡后延迟关闭；滚动页面时关闭，避免位置漂移。

### 5.2 换绑代理弹窗

点击“换绑代理”打开独立工作台弹窗，不替换页面中的代理摘要。弹窗打开时默认选中“本地网络”，关闭或取消后页面代理草稿保持不变。

#### 5.2.1 弹窗整体布局

```text
┌──────────────────────────────────────────────────────────────────────┐
│ 换绑代理                                                       [关闭]│
├──────────────────────────────────────────────────────────────────────┤
│ 代理类型： [本地网络] [平台代理] [自有代理] [代理 API]              │
│ IP 查询渠道：[IP2Location                                      ▾]    │
│                                                                      │
│ 代理列表： [购买/添加代理] [检测当前页代理]                           │
│            ┌─ 筛选、搜索与刷新 ──────────────────────────────────┐   │
│            │ 列表表头（粘滞） / 数据行 / 空状态                  │   │
│            │ 分页器                                               │   │
│            └─────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────────────┤
│ 已选摘要                                             [取消] [确定]    │
└──────────────────────────────────────────────────────────────────────┘
```

- 弹窗宽度为 `min(1440px, 96vw)`，高度为 `min(900px, 92vh)`；圆角 8px。
- 标题栏高 56px，底部操作栏高 64px，均固定；中间内容区独立滚动。
- 字段标签列宽 112px，标签与控件间距 14px；代理类型分段控件、IP 查询渠道下拉框宽 400px，字段垂直间距 16px。
- IP 查询渠道仅显示下拉框，不显示安全图标；参数固定为 `IP2Location`、`IPData`，默认 `IP2Location`。
- 底部左侧显示当前选择摘要；右侧为“取消”“确定”。本地网络无需选择记录即可确定，其他类型未选择记录时“确定”禁用。

#### 5.2.2 四种代理类型

| 类型 | 默认内容与主操作 | 筛选项 | 列表字段 |
|---|---|---|---|
| 本地网络 | 居中显示网络图标和“本地网络无需选择代理”；不显示代理列表 | 无 | 无 |
| 平台代理 | “购买代理”“检测当前页代理” | 平台类型、地区、状态、搜索、刷新 | 单选、序号、代理名称、代理信息、服务商、地区、标签、状态、环境数、到期时间 |
| 自有代理 | “添加自有代理”“检测当前页代理” | 代理类型、地区、状态、搜索、刷新 | 单选、序号、代理名称、代理信息、代理类型、环境数、备注、到期时间、操作 |
| 代理 API | “添加代理 API”“检测当前页代理” | 代理类型、地区、状态、搜索、刷新 | 单选、序号、代理名称、代理类型、提取链接、提取方式、代理检测结果、环境数、操作 |

“购买代理”按钮使用购物袋图标，点击后在新标签页打开商城模块；原换绑代理弹窗保持当前状态，不在当前页内替换内容。

#### 5.2.3 筛选、列表与分页规则

- 平台类型参数为“全部平台类型、静态住宅、动态住宅、ISP”；自有代理和代理 API 的类型参数为“全部代理类型、HTTP、HTTPS、SOCKS5”。
- 地区默认“全部地区”；数据由地区枚举接口提供。
- 状态参数只能为“全部状态、未绑环境、已绑环境”。环境数为 0 时状态为“未绑环境”，环境数大于 0 时状态为“已绑环境”；换绑或自动分配成功后，该代理的环境数和状态同步更新。
- 搜索框固定宽 400px，支持序号、代理名称和 IP；输入后 300ms 防抖并回到第 1 页。
- 代理信息字段在 IP 后显示刷新图标；点击只刷新该行代理信息并展示行内加载状态，不改变选中项。
- 序号和到期时间支持升序、降序切换；首次进入默认按序号升序。不存在到期时间列的类型只提供序号排序。
- 点击单选框或数据行选择代理；选中行高亮。过期、不可用或无权限记录不得选择，并需显示具体原因。
- 表头在列表视口内粘滞；表格最小宽度 1080px，仅表格视口横向滚动。
- 分页默认 10 条/页，可切换 20、50 条/页；显示页码、上一页、下一页、总记录数和当前页数。
- “检测当前页代理”只检测当前分页内可见记录；检测中禁用重复点击，完成后逐行更新结果。

#### 5.2.4 确定、取消与类型切换

- 切换代理类型时保留各类型最近一次临时选中项，但不得自动提交上一类型选择。
- 点击“确定”仅把代理和 IP 查询渠道写入编辑浏览器草稿，并关闭弹窗；仍需点击页面“保存修改”才持久化。
- 点击“取消”、右上角关闭按钮、遮罩或按退出键，均丢弃本次未确认选择并回到原代理摘要。
- 关闭后焦点回到“换绑代理”按钮；确定后若勾选“修改 IP 后自动重新生成指纹项”，只触发一次相关指纹生成。
- 代理账号、密码和提取链接在列表、日志、埋点及错误信息中必须脱敏。

#### 5.2.5 添加或编辑自有代理弹窗

在“自有代理”类型下点击“添加自有代理”打开二级弹窗；编辑列表记录复用同一弹窗并回填数据。

| 字段 | 控件与默认值 | 规则 |
|---|---|---|
| 代理名称 | 文本框，最多 100 字 | 必填；实时显示字数 |
| 代理类型 | 下拉框，默认 HTTP | 参数为 HTTP、HTTPS、SOCKS5、SSH |
| 地址类型 | 下拉框，默认 IPv4 | 参数为 IPv4、IPv6、域名 |
| 代理 IP 或域名 | 地址输入框 + 端口输入框 | 必填；端口为 1–65535 的整数 |
| 代理账号 | 文本框 | 可选；不得写入埋点 |
| 代理密码 | 密码框 | 可选；默认掩码，不回显明文 |
| 备注 | 文本框，最多 40 字 | 可选；实时显示字数 |
| 到期时间 | 日期时间选择器 | 可选；已过期时列表明确标识且不可绑定 |
| IP 查询渠道 | 下拉框 | 参数为 `IP2Location`、`IPData` |
| 检测代理 | 次按钮 | 根据当前表单执行检测，结果在按钮右侧展示 |

- 弹窗宽 760px，最大宽度 94vw、最大高度 92vh；表单标签列 126px，字段间距 14px。
- 底部左侧为“校验重复项”复选框，右侧依次为“取消”“添加”“添加并绑定”。
- “添加”成功后更新自有代理列表但不改变当前绑定；“添加并绑定”成功后同时选中该代理并写入页面草稿。

#### 5.2.6 添加或编辑代理接口弹窗

在“代理 API”类型下点击“添加代理 API”打开二级弹窗；编辑列表记录复用同一弹窗并回填数据。

| 字段 | 控件与默认值 | 规则 |
|---|---|---|
| 代理名称 | 文本框，最多 100 字 | 必填 |
| 代理类型 | 下拉框，默认 HTTP | 必填；参数为 HTTP、HTTPS、SOCKS5 |
| 提取方式 | 下拉框 | 必填；每次打开浏览器提取新 IP、每次启动时提取、固定间隔自动提取 |
| 提取链接 | 文本框 | 必填；日志和列表默认脱敏 |
| IP 查询渠道 | 下拉框 | 参数为 `IP2Location`、`IPData` |
| 检测代理 | 主按钮 | 根据当前提取配置检测，结果在按钮右侧展示 |

- 弹窗宽 760px，内容区独立滚动；底部固定“取消”“确定”。
- 表单下方展示白名单注意事项与客服入口；说明文字不得替代字段校验或检测结果。
- 保存成功后刷新代理 API 列表；若由编辑入口打开，则更新原记录而不是新增重复记录。

### 5.3 自动分配代理

点击后按团队、地区、代理池可用状态与绑定限制，从“未绑环境”的平台代理中分配。按钮进入加载状态并禁止重复点击。成功后更新代理草稿，同时将该代理环境数至少更新为 1、状态更新为“已绑环境”；失败区分“无可用代理”“余额不足”“代理池不可用”“网络超时”“无权限”。若勾选自动重生成指纹，在代理分配成功后触发一次指纹生成，不得重复触发。

### 5.4 账号平台入口与已绑定列表

```text
账号平台： [添加新账号] [选择已有账号]  账号平台有什么用？
            ┌─────────────────────────────────────────────────────┐
            │ 平台        名称        账号        操作             │
            │ 平台图标    账号名称    脱敏账号    修改  解绑       │
            └─────────────────────────────────────────────────────┘
```

- “添加新账号”“选择已有账号”组成 500px 双按钮组；“账号平台有什么用？”紧邻“选择已有账号”右侧并保持页面原位置，不放入弹窗。
- 未绑定账号时不显示列表；添加或选择完成后，在入口下方显示已绑定账号列表，最大宽度 920px。
- 列表字段固定为平台、名称、账号、操作。平台列显示平台图标和完整名称；超长文本单行省略并可悬停查看全文。
- 点击“修改”打开编辑账号弹窗，复用添加账号表单并回填当前参数；保存后同步更新列表。
- 点击“解绑”直接从当前浏览器草稿列表移除，不删除账号资产；操作后标记页面为未保存状态。

### 5.5 添加或编辑账号弹窗

点击“添加新账号”打开添加弹窗；点击已绑定列表中的“修改”打开编辑弹窗。两者复用同一表单，区别仅为标题、默认值和主按钮文案。

| 字段 | 类型 | 规则 |
|---|---|---|
| 平台 | 可搜索下拉选择 | 必填；展示平台及站点/国家；枚举由接口提供，自定义平台允许配置名称和登录网址 |
| 名称 | 文本框 | 可选；最多 100 字；为空时默认使用平台名称 |
| 账号 | 文本框 | 必填；不得进入埋点、网址或控制台日志 |
| 密码 | 密码框 + 显隐按钮 | 默认掩码；仅有权限用户可临时查看；不得明文回显或持久化到前端缓存 |
| 2FA 密钥 | 密码框 + 显隐按钮 | 默认掩码；格式校验只给客户端提示，不记录原值 |
| 备注 | 文本框 | 可选；最多 80 字 |
| 锁定账密 | 复选框，默认关闭 | 开启后普通成员不可查看或修改密码和双重验证密钥 |

- 弹窗宽 760px、最大宽度 94vw、最大高度 88vh；标题栏和底栏固定，内容区独立滚动。
- 表单内容宽 560px，标签列 126px，输入框和下拉框统一宽 400px，字段间距 16px。
- 底部左侧提供“选择已有账号”入口；右侧为“取消”和“添加并绑定”或“保存修改”。
- 点击“选择已有账号”时先关闭当前弹窗，再打开选择已有账号弹窗；未提交的新增或编辑草稿不保留。
- 添加成功后新账号直接加入当前浏览器草稿，并立即显示在已绑定列表；编辑成功后只更新原账号记录。
- 点击取消、关闭按钮、遮罩或按退出键关闭弹窗，不保存本次输入；关闭后焦点回到原触发入口。

### 5.6 选择已有账号弹窗

选择已有账号使用独立列表弹窗，不提供平台类型、地区或状态筛选条件。

```text
┌──────────────────────────────────────────────────────────────────┐
│ 选择已有账号                                                [关闭]│
├──────────────────────────────────────────────────────────────────┤
│ [添加新账号] [搜索名称、账号、备注（400px）]             [刷新] │
├──────────────────────────────────────────────────────────────────┤
│ □ 序号 平台 名称 账号 地区 备注 锁定状态                         │
│ 数据列表（支持跨页多选，表头粘滞）                               │
├──────────────────────────────────────────────────────────────────┤
│ 页码 / 每页条数 / 总数                                           │
├──────────────────────────────────────────────────────────────────┤
│ 已选：N 个账号                                      [取消] [确定]│
└──────────────────────────────────────────────────────────────────┘
```

- 弹窗宽度为 `min(1200px, 96vw)`，高度为 `min(820px, 88vh)`；标题栏和底栏固定，列表视口独立滚动。
- 工具栏从左到右为“添加新账号”、400px 搜索框、刷新按钮；搜索支持名称、账号、备注，输入后回到第 1 页。
- 点击“添加新账号”时先关闭当前弹窗，再打开添加账号弹窗；未确认的临时选择不提交。
- 列表字段为当前页全选、序号、平台、名称、账号、地区、备注、锁定状态；账号、邮箱和手机号按查看权限脱敏。
- 支持单行点击、复选框、当前页全选和跨页多选；已选数量与最多前三个账号名称在底部实时更新。
- 锁定账号可按权限选择，但不可查看或编辑密码、双重验证密钥。
- 分页默认 10 条/页，可切换 20、50 条/页；空结果显示“暂无符合条件的账号”并保留分页摘要。
- 点击“确定”以当前选择替换浏览器草稿中的绑定账号集合；点击“取消”、关闭按钮、遮罩或按退出键丢弃临时选择。

### 5.7 基础设置

| 字段 | 实测选项/行为 | 校验与联动 |
|---|---|---|
| 浏览器内核 | Chromium 147(beta)、146、145、144、143、142、141、140、134、131、127、107 | 必填；beta 标识不可省略；切换时刷新兼容的 UA 版本 |
| 操作系统 | 自定义下拉（隐藏原生 select），按系统分组展示：All Windows / Windows 11 / 10 / 8.1 / 8 / 7；All MacOS / MacOS 15 / 14 / 13 / 12 / 11 / 10；Linux；All Android / Android 13 / 12 / 11 / 10 / 9；All iOS / iOS 16 / 15 / 14。每项显示对应系统图标与名称，Windows/MacOS/Linux/Android/iOS 各自使用专属图标颜色；默认选中 Windows 10 并以蓝色高亮 + 勾选标记 | 当前生效 OS 必须唯一；移动 OS 与桌面内核/UA 组合需校验；切换 OS 时联动生成匹配的 UA 平台片段（如 Windows 10/11 生成 `Windows NT 10.0; Win64; x64`，MacOS 生成 `Macintosh; Intel Mac OS X`）；随机生成指纹时 OS 与内核、UA 同步覆盖 |
| UA 版本 | 多选下拉（隐藏原生 select，`multiple`），可选 100–147；默认选中 134。选中项以 `参数[值]` 形式展示，每项附 `x` 移除按钮；当选中超过 2 个时，控件内直显前 2 个参数标签，剩余折叠为 `[+n]`（n 为剩余项数量），点击展开查看全部。配合完整 UA 输入框 + 刷新图标 | 版本必须与内核主版本匹配；多选值均参与 UA 生成；完整 UA 非空；刷新后更新摘要 |
| 分组 | 自定义下拉（隐藏原生 select，宽 500px），面板顶部含“新建分组”输入框（`maxlength=30`，含 `0 / 30` 字数计数）、“添加”按钮（输入为空时禁用）。支持回车键或点击“添加”保存；新建分组追加到列表底部；重复名称不允许。列表项支持删除操作，默认分组不可删除；删除当前选中分组时自动切换回默认分组 | 单选；无权限分组禁用；分组名称 ≤ 30 字 |
| 标签 | 自定义下拉（隐藏原生 select，宽 500px），未选择时显示“请选择”占位。面板顶部含“新建标签”输入框（`maxlength=30`，含 `0 / 30` 字数计数）、“添加”按钮（输入为空时禁用）。支持回车键或点击“添加”保存；新建标签随机分配背景色与字体色；重复标签不允许。已建标签以胶囊形式展示，每项附 `x` 删除按钮，删除不影响历史已绑定标签 | 多选；允许为空；支持清除；标签名称 ≤ 30 字 |
| Cookie | 文本域 | 支持 JSON、Netscape、Name=Value；解析失败精确到行；内容默认不回显到日志 |
| 添加其他 Cookie | 动态新增 | 每组有来源/域名和删除入口；至少保留一组 |
| 打开网址 | 多行文本域 | 每行一个网址；允许无协议输入并规范化为 HTTPS；无效行提示行号 |
| 备注 | 文本域 + `0 / 500` | 最多 500 字；允许为空 |

### 5.8 指纹设置

| 字段 | 实测选项 | 默认摘要/规则 |
|---|---|---|
| 语言 | 基于 IP 生成、自定义 | 默认基于 IP；自定义支持语言优先级 |
| 时区 | 基于 IP 生成、自定义 | 默认基于 IP；自定义使用 IANA 时区 |
| 地理位置权限 | 询问、允许、禁止 | 实测摘要为允许 |
| 地理位置来源 | IP 匹配、自定义 | 自定义时显示经纬度；纬度 -90~90，经度 -180~180 |
| 屏幕分辨率模式 | 随机、快捷选择、自定义 | 默认摘要为默认；自定义宽高为正整数 |
| 字体列表 | 默认、自定义 | 与 OS 联动；自定义至少一个字体 |
| ClientRects | 开启、关闭 | 实测默认开启 |
| WebRTC | 替换、真实、禁用 | 实测默认禁用；“真实”需风险提示 |
| Canvas | 噪声、真实 | 实测为噪声策略 |
| Canvas 倾向 | 倾向一致性、倾向随机性 | 实测摘要为倾向一致性 |
| WebGL | 噪声、真实 | 实测默认噪声 |
| WebGL Info | 自定义、真实 | 实测默认自定义 |
| WebGL 厂商 | 下拉 | 必须与渲染器和 OS 合理匹配 |
| WebGL 渲染 | 文本 + 随机生成 | 非空；最大 500 字；不得包含控制字符 |
| AudioContext | 噪声、真实 | 实测摘要为真实 |
| SpeechVoices | 噪声、真实 | 实测摘要为噪声 |
| 媒体设备 | 噪声、真实 | 实测摘要为真实 |
| 硬件并发数 | 下拉 | 实测默认 4；建议枚举 2/4/8/12/16/24/32 |
| 设备内存 | 下拉 | 实测默认 8；建议枚举 2/4/8/16/32/64 GB |
| 设备名称 | 自定义、关闭 | 自定义时文本必填；提供随机生成 |
| MAC 地址 | 自定义、关闭 | `XX-XX-XX-XX-XX-XX`；提供随机生成；禁止组播/全零地址 |
| 硬件加速 | 默认、开启、关闭 | 实测摘要为开启 |
| 蓝牙 | 噪声、真实 | 实测摘要为噪声 |
| Do not track | 默认、开启、关闭 | 实测摘要为默认 |
| 电池 | 噪声、真实 | 实测摘要为真实 |
| 端口扫描防护 | 开启、关闭 | 实测摘要为开启 |
| 白名单 | 端口文本 | 1–65535 的整数，半角逗号分隔，如 `4000,4001`；去重并拒绝空项、范围外值 |

### 5.9 插件选择

- 插件分组触发器宽 500px，未选择时显示“请选择插件分组”；点击后在区块内展开 `500 × 280px` 双栏面板；
- 面板左栏宽 200px，提供“团队分组 / 我的分组”切换；右栏展示当前范围下的分组，空集合时在内容区顶部显示“暂无数据”；
- 面板参与文档流：展开时插件选择区块自动增高并将“全局插件”字段向下推移，关闭时自动收回，不得遮挡、裁切或超出区块；
- “插件分组”与“全局插件”字段垂直间距固定为 16px；
- 全局插件字段使用安全项图标，默认展示“元素选择器”图标和灰色省略号按钮；点击省略号后在区块内展开 210px 宽菜单，菜单项“元素选择器”保持单行；
- 插件分组面板与全局插件菜单互斥；点击外部、按 Escape 或切换另一个浮层时关闭，并刷新区块高度和交互标注位置；
- 页面不再展示 Cookie 助手、网页翻译、广告像素检查器、移动端调试等逐插件开关卡片；
- 不在此页执行插件安装、上传、审核或更新，只展示团队分组选择与已注入的全局插件入口。

### 5.10 偏好设置

| 字段 | 实测选项 | 说明 |
|---|---|---|
| 网页通知 | 询问、禁止通知 | 对应浏览器通知权限 |
| 禁止加载图片 | 跟随团队、自定义、关闭 | 自定义时提供例外域名或规则入口 |
| 禁止加载视频 | 跟随团队、开启、关闭 | “开启”表示禁止加载视频 |
| 禁止播放声音 | 跟随团队、开启、关闭 | “开启”表示静音策略 |
| 定时内存回收 | 跟随团队、开启、关闭 | 开启时按团队周期执行 |
| 自动删除缓存 | 跟随团队、开启、关闭 | 在环境关闭或约定时机执行 |
| 自动删除 Cookie | 跟随团队、开启、关闭 | 高风险；开启需提示会导致登录态丢失 |
| 随机生成指纹 | 跟随团队、开启、关闭 | 指环境启动时是否自动生成，不等同于右侧即时随机按钮 |

“跟随团队”必须在旁边展示当前继承的最终值，例如“跟随团队（开启）”，避免用户无法判断实际行为。

偏好设置区块标题不显示额外“可继承”标签；字段行之间不显示分割线，仅使用 16px 垂直间距形成层级。

### 5.11 指纹信息摘要

右侧 sticky 面板展示最终值：浏览器内核、UA、语言、时区、地理位置、屏幕分辨率、字体列表、ClientRects、WebRTC、Canvas、WebGL、WebGL Info、AudioContext、SpeechVoices、媒体设备、硬件并发数、设备内存、设备名称、MAC 地址、硬件加速、蓝牙、Do Not Track、电池、端口扫描防护。

摘要更新规则：

- 用户修改字段后 100ms 内同步；
- “基于 IP”或“随机”项展示策略和最终解析值，例如“基于 IP（Asia/Shanghai）”；
- 无网络检测结果时展示“待检测”，不能伪造地理值；
- 值被错误阻塞时显示错误标识和来源字段；
- 长 UA、WebGL 渲染器使用两行截断，悬浮显示全文；
- 支持复制非敏感摘要；MAC 和设备名默认可复制，账号、Cookie、密码、2FA 不进入摘要。

### 5.12 随机生成新指纹

实测点击后立即改变硬件并发数、设备内存、设备名称和 MAC 地址，未出现确认弹窗。原型已移除“恢复上一套指纹”按钮与对应 `captureFingerprintConfig` / `restoreFingerprintConfig` 逻辑，命令区不再展示进度条与“设备画像一致”文字。目标产品行为定义为：

1. 首次未做手动修改时可立即生成；
2. 已存在手动指纹草稿时弹出确认：“随机生成会覆盖当前自定义指纹配置，是否继续？”；
3. 生成范围覆盖基础设置、指纹设置、插件选择和偏好设置，并覆盖这些区域的现有草稿值（OS、内核、UA 同步覆盖）；
4. 名称、代理信息、IP 查询渠道、账号平台等环境信息不受影响；
5. 与当前代理地区形成一致画像，生成后的 OS、内核、UA 与硬件参数必须彼此一致；
6. 支持多次生成；每次都以当前生效草稿为判断基线，存在人工编辑时再次确认；
7. 生成只修改草稿，必须点击页面“保存修改”后持久化；
8. 生成后不提供“撤销随机生成”或“恢复上一套指纹”能力，已覆盖的草稿值无法回退到生成前状态。

## 6. 状态模型与业务逻辑

### 6.1 页面状态

```text
loading -> ready
loading -> loadError -> retry -> loading
ready -> dirty -> validating -> saving -> success -> environmentList
ready -> dirty -> validating -> validationError -> dirty
ready -> dirty -> saving -> saveError -> dirty
ready/dirty -> cancel -> environmentList
```

### 6.2 核心状态对象

```ts
type EditBrowserState = {
  source: BrowserEnvironment;
  draft: BrowserEnvironmentDraft;
  dirtyFields: Set<string>;
  expandedSections: Set<'basic' | 'fingerprint' | 'plugins' | 'preferences'>;
  proxyDialog: {
    open: boolean;
    type: 'local' | 'platform' | 'owned' | 'api';
    selectionByType: Record<string, ProxyBinding | null>;
  };
  accountDialog: 'closed' | 'create' | 'edit' | 'select';
  editingAccountId?: string;
  accountSelectionDraft: Set<string>;
  networkCheck: { status: 'idle' | 'loading' | 'success' | 'error'; result?: NetworkResult };
  validationErrors: Record<string, string>;
  saveStatus: 'idle' | 'validating' | 'saving' | 'success' | 'error';
  configEdited: boolean;
};
```

### 6.3 联动优先级

1. 权限与团队策略；
2. 用户明确自定义值；
3. 当前代理/IP 检测结果；
4. OS + 内核 + UA 一致性；
5. 团队默认；
6. 系统随机值。

### 6.4 脏数据规则

- 与 `source` 深比较后决定是否 dirty；
- 展开/折叠、搜索、分页、悬浮不产生 dirty；
- 网络检测结果不单独产生 dirty；
- 换绑代理弹窗内搜索、分页、排序、检测和临时选择不产生 dirty；点击“确定”合并到页面草稿后产生 dirty；
- 添加代理资产但未绑定时不改变浏览器草稿；“添加并绑定”或换绑确定后产生 dirty；
- 添加并绑定账号、保存账号修改、确认选择已有账号或解绑账号后产生 dirty；
- 添加账号与选择已有账号弹窗之间切换不产生 dirty，且不保留未提交的当前弹窗草稿；
- 随机生成指纹立即产生 dirty；
- 基础设置、指纹设置、插件选择或偏好设置任一人工修改后，`configEdited=true`；随机生成按钮据此决定是否二次确认；
- 保存成功后以响应数据替换 source 并清空 dirty。

## 7. 校验、错误与边界

### 7.1 校验时机

- 输入中：字数、明显格式错误；
- 失焦：字段级校验；
- 子流程确定：代理或账号内部完整校验；
- 页面保存修改：全量同步校验；
- 服务端：权限、唯一性、代理可用性、版本与策略校验。

### 7.2 错误呈现

- 字段错误显示在字段下方并设置 `aria-describedby`；
- 折叠区内有错误时自动展开，并在标题显示错误数量；
- 提交失败聚焦第一个错误，顶部展示错误摘要；
- 网络错误不清空草稿；
- 401 跳转登录前将非敏感草稿存入 sessionStorage；敏感值不落盘；
- 409 表示环境已被他人修改，展示“重新加载 / 保留并覆盖（有权限）”；
- 429 展示稍后重试和剩余时间；
- 5xx 保留草稿并允许重试。

### 7.3 一致性校验

- Chromium 主版本与 UA Chrome 主版本一致；
- UA 平台片段与 OS 一致；
- 移动 OS 需使用移动 UA 与合理分辨率；
- WebGL 厂商与渲染器成对；
- 设备内存、硬件并发数不应形成明显异常组合；
- 时区、语言、定位与代理地区冲突时给出风险提示，但允许有权限用户保存；
- WebRTC 为真实且使用代理时显示真实 IP 泄漏风险；
- 自动删除 Cookie 与导入 Cookie 同时启用时提示其生命周期。

## 8. 保存、取消与离开保护

### 8.1 保存修改

1. 点击“保存修改”后执行全量校验；
2. 校验失败自动展开相关分区并定位首个字段；
3. 校验通过后按钮变为“保存中…”且禁用重复提交；
4. 使用环境版本号做乐观锁；
5. 保存成功 Toast“编辑浏览器成功”，随后返回环境管理并高亮该行；
6. 保存失败保留草稿和滚动位置。

### 8.2 取消

实测行为是直接放弃修改并返回环境管理，没有二次确认。目标实现建议保留实测兼容开关：

- `confirmDiscard=false`：直接取消，与 v4.0.2.4 一致；
- `confirmDiscard=true`（推荐）：dirty 时弹窗“当前修改尚未保存，是否放弃？”；未修改直接返回。

智能编码首版使用推荐值 `true`，弹窗按钮为“继续编辑 / 放弃修改”。

### 8.3 其他离开方式

侧栏跳转、浏览器返回、关闭标签页、刷新页面均复用离开保护。`beforeunload` 仅在 dirty 时注册。敏感字段不得写入自动恢复缓存。

## 9. 数据模型与接口建议

### 9.1 主要模型

```ts
type BrowserEnvironmentDraft = {
  id: string;
  version: number;
  name: string;
  proxy: ProxyBinding;
  regenerateIpFingerprint: boolean;
  ipLookupProvider: string;
  accountIds: string[];
  basic: BasicSettings;
  fingerprint: FingerprintSettings;
  pluginGroupId?: string;
  pluginOverrides: Record<string, boolean>;
  preferences: PreferenceSettings;
};
```

### 9.2 接口清单

| 请求方法 | 接口路径 | 用途 |
|---|---|---|
| GET | `/api/browser-environments/{id}` | 获取编辑详情、权限和版本号 |
| PATCH | `/api/browser-environments/{id}` | 保存变更；携带 `version` 或 `If-Match` |
| POST | `/api/network/check` | 检测当前草稿代理，返回 IP、地区、延迟 |
| GET | `/api/proxies` | 按类型和筛选获取代理 |
| POST | `/api/proxies/auto-assign` | 预分配代理，返回候选绑定，不直接保存环境 |
| POST | `/api/proxies/check` | 检测选定或当前页代理 |
| POST/PATCH | `/api/proxies/owned/{id?}` | 新增或编辑自有代理 |
| POST/PATCH | `/api/proxy-apis/{id?}` | 新增或编辑代理接口 |
| GET | `/api/accounts` | 搜索已有账号；按权限脱敏 |
| POST | `/api/accounts` | 创建账号；敏感字段加密传输和存储 |
| PATCH | `/api/accounts/{id}` | 更新账号非敏感信息或覆盖敏感凭证 |
| GET | `/api/platforms` | 获取平台分类、站点与国家枚举 |
| POST | `/api/fingerprints/generate` | 按 OS、内核、UA、IP 与锁定项生成一致指纹 |
| GET | `/api/plugin-groups` | 获取插件分组与插件兼容性 |
| GET | `/api/team-preferences` | 获取偏好继承值 |

所有写接口返回 `requestId`，错误采用 `{ code, message, fieldErrors, requestId }`。前端不得根据中文 message 判断业务分支。

## 10. 权限、安全与审计

- 页面加载时返回字段级权限：view/edit/reveal；
- 密码与 2FA 只允许覆盖更新，不从接口返回明文；
- Cookie 默认仅显示“已配置”与条目数，需权限才能重新录入；
- 代理认证、API 提取链接脱敏；
- 所有传输使用 TLS；敏感值服务端加密存储；
- 前端错误监控、埋点、控制台、URL、localStorage 禁止包含密码、2FA、Cookie、账号全文、代理密钥；
- 审计日志记录操作者、环境 ID、字段路径、变更时间和结果；敏感字段仅记录“已更新”，不记录旧值和新值；
- 锁定账密的权限变更属于单独审计事件；
- 防止重复提交、CSRF、越权 ID 替换与返回数据过度暴露。

## 11. 埋点

| 事件 | 触发 | 非敏感属性 |
|---|---|---|
| `edit_browser_view` | 页面成功加载 | environment_id、role、version |
| `edit_section_toggle` | 展开/折叠 | section、expanded |
| `proxy_picker_open` | 打开换绑 | current_type |
| `proxy_type_change` | 切换代理类型 | type |
| `network_check_result` | 检测结束 | success、provider、latency_bucket、error_code |
| `account_mode_change` | 添加/选择账号 | mode |
| `fingerprint_generate` | 随机生成 | trigger、changed_field_count、success |
| `edit_browser_submit` | 点击确定 | dirty_field_count、validation_error_count |
| `edit_browser_result` | 保存结束 | success、error_code、duration_bucket |
| `edit_browser_cancel` | 取消 | dirty、confirm_shown |

## 12. 实现决策

- 采用单一 `draft` 状态作为表单事实来源，右侧摘要由 selector 计算，禁止维护第二份独立摘要状态；
- 代理与账号使用独立弹窗子状态机；弹窗临时状态与主草稿隔离，只有“确定”“添加并绑定”或账号保存成功后才合并；
- 换绑代理工作台采用固定标题栏、独立滚动内容区和固定底栏；添加代理二级弹窗高于工作台弹窗，关闭后返回原代理类型与筛选状态；
- 添加/编辑账号弹窗与选择已有账号弹窗互斥，切换入口必须先关闭当前弹窗，防止两个账号弹窗叠加；
- 折叠区使用统一 Accordion 组件，允许多个分区同时展开；
- 分段按钮使用单选语义，二态开关不滥用 Checkbox；分段按钮（segment）统一设置 `min-width:100px` 并按内容自适应宽度，左右内边距 14px，禁止等分拉伸；
- 操作系统、UA 版本、分组、标签均使用自定义下拉（隐藏原生 select，承载表单值与 change 事件），由自定义控件层提供交互、样式与键盘支持；
- UA 版本采用多选语义，选中项以“参数[值]”胶囊展示并支持单点移除；超过 2 个时直显前 2 个并以 `[+n]` 折叠剩余项，避免控件溢出 500px 控件区；
- 分组与标签下拉内置“新建”入口：输入框 `maxlength=30` + 实时字数计数 + 禁用态“添加”按钮，支持回车或点击保存；分组新建项追加到列表底部，标签新建项随机分配背景色与字体色；二者均做重复校验。分组支持删除用户创建项（默认分组不可删，删除当前选中项时回退到默认分组），标签支持逐项删除且不影响历史已绑定标签；
- 枚举由接口或 Mock 配置驱动，不将平台、内核、国家、时区、插件硬编码在组件中；
- 统一 FormField、Select、SegmentedControl、SensitiveField、DataPicker、FingerprintSummary、StickyActionBar 组件；
- 配置型表单统一使用 120px 标签列、500px 控件区和 16px 字段间距；一个字段占一行，按钮和帮助提示不计入控件宽度；
- 插件分组使用参与文档流的双栏选择面板，全局插件使用参与文档流的单列菜单；两者互斥打开并在开合后重算区块高度与标注位置；
- “自定义指纹配置”命令区仅保留区块标题、说明文字、“已自定义”徽标和“随机生成新指纹”按钮；不渲染进度条、“设备画像一致”文字、“恢复上一套指纹”按钮，也不实现 `captureFingerprintConfig` / `restoreFingerprintConfig`；随机生成后不保留可撤销快照；
- 帮助说明统一使用可悬停、可聚焦、可停留操作的气泡；帮助中心链接以蓝色高亮并打开帮助中心页面；
- 标注系统默认隐藏，只查询编辑浏览器业务内容和本页底部操作栏；系统框架区域不生成徽标，业务编号按阅读顺序连续为 1–16；
- 网络检测和保存使用独立 AbortController；离开页面时取消未完成请求；
- 所有列表分页从 1 开始，空态仍显示总数与禁用分页；
- 原型 Mock 必须提供成功、空数据、无权限、超时、服务错误和冲突六类切换入口；
- 本功能最高测试缝为“编辑浏览器页面状态机 + Mock API 适配器”，外部行为通过页面级测试覆盖，避免逐个内部函数测试。

## 13. 测试决策

测试只验证用户可观察行为，不依赖组件内部实现。以完整页面为主要测试缝，Mock API 控制加载、保存、网络检测、代理、账号、指纹生成和错误响应。

### 13.1 核心测试用例

| 编号 | 场景 | 操作 | 预期 |
|---|---|---|---|
| EB-001 | 页面加载 | 打开有效环境 | 原配置、权限、摘要和版本正确显示 |
| EB-002 | 名称为空 | 清空并提交 | 显示必填错误，阻止请求 |
| EB-003 | 名称过长 | 输入 101 字 | 显示 `101 / 100` 与错误，不截断输入，阻止提交 |
| EB-004 | 名称边界 | 输入 100 字 | 无长度错误，可提交 |
| EB-005 | 网络检测成功 | 点击检查网络 | loading 后展示 IP、地区、渠道与延迟 |
| EB-006 | 网络检测失败 | Mock 超时 | 保留草稿，显示可重试错误 |
| EB-007 | 代理四模式 | 打开弹窗并依次切换 | 默认本地网络；字段、表头、空态和主操作符合当前类型 |
| EB-008 | 代理筛选 | 切换类型、地区、状态并输入关键词 | 状态仅有全部、未绑、已绑三项；防抖查询后结果与总数更新 |
| EB-009 | 换绑取消 | 选择代理后取消 | 恢复原代理摘要，不产生 dirty |
| EB-010 | 换绑确定 | 选择代理后确定 | 草稿更新，页面未立即持久化 |
| EB-011 | IP 联动 | 勾选自动生成后换绑 | 成功绑定后仅触发一次指纹生成 |
| EB-012 | 添加账号校验 | 缺少必填点击确定 | 精确提示字段，不发送请求 |
| EB-013 | 敏感字段 | 输入密码/2FA | 掩码；日志、埋点、URL 无原值 |
| EB-014 | 已有账号 | 搜索、分页、选择 | 数据脱敏，选择结果进入草稿 |
| EB-015 | 内核与 UA | 改内核 | UA 候选和摘要联动 |
| EB-016 | OS 切换 | Windows 改为移动 OS | UA、分辨率候选变化，不一致项提示 |
| EB-017 | Cookie 格式 | 输入三种合法格式 | 均可解析；非法格式显示行号 |
| EB-018 | 多网址 | 输入合法/非法混合行 | 非法行定位，合法无协议自动规范化 |
| EB-019 | 指纹各分段 | 逐项切换 | 选中态、附加字段和摘要正确 |
| EB-020 | 定位自定义 | 输入边界经纬度 | 合法可保存，越界阻止 |
| EB-021 | MAC 校验 | 输入非法 MAC | 显示格式错误，阻止保存 |
| EB-022 | 端口白名单 | 输入 `1,65535` | 通过；0、65536、空项、中文逗号失败 |
| EB-023 | 随机指纹 | 点击生成 | 指纹草稿变化，业务字段不变；命令区无进度条、无“设备画像一致”文字、无“恢复上一套指纹”按钮；生成后无撤销入口 |
| EB-024 | 随机覆盖确认 | 已手动改指纹后生成 | 展示确认；取消不变；继续才覆盖 |
| EB-025 | 插件兼容 | 选择不兼容内核 | 插件禁用并显示原因 |
| EB-026 | 团队偏好 | 选择跟随团队 | 展示继承的最终值 |
| EB-027 | 保存成功 | 修改后确定 | 单次请求，Toast 成功，返回列表并高亮 |
| EB-028 | 保存失败 | Mock 500 | 保留草稿与滚动位置，可重试 |
| EB-029 | 并发冲突 | Mock 409 | 展示重载/覆盖方案，不静默覆盖 |
| EB-030 | 取消无修改 | 点击取消 | 直接返回列表 |
| EB-031 | 取消有修改 | 点击取消 | 推荐模式出现确认，放弃后返回 |
| EB-032 | 离开保护 | dirty 后刷新/返回/侧栏跳转 | 统一提示，选择继续编辑时保持状态 |
| EB-033 | 键盘 | 仅键盘走主流程 | 焦点顺序正确，弹层焦点锁定并归还 |
| EB-034 | 只读权限 | 只读角色进入 | 可查看摘要，所有写操作禁用 |
| EB-035 | 响应式 | 1440/1280/1024/768 宽度 | 无横向遮挡，摘要按规则降级 |
| EB-036 | 帮助气泡 | 悬停或聚焦代理、IP、账号说明 | 文案完整；气泡可停留；帮助中心链接可操作；账号帮助悬停不变蓝 |
| EB-037 | 插件分组面板 | 点击插件分组 | 展开 500×280 双栏面板；左栏 200px；右栏空态“暂无数据”；区块自动增高 |
| EB-038 | 插件范围切换 | 切换团队分组/我的分组 | 唯一激活态正确，内容区同步且不产生 dirty |
| EB-039 | 全局插件菜单 | 点击省略号 | 展开 210px 菜单；“元素选择器”单行；菜单完整包含在区块内 |
| EB-040 | 插件浮层互斥 | 依次打开分组面板和全局插件菜单 | 后打开者显示，前一个关闭；字段间距始终为 16px |
| EB-041 | 随机覆盖范围 | 修改四个配置区后确认随机生成 | 覆盖基础、指纹、插件、偏好；名称、代理、IP 渠道、账号平台不变 |
| EB-042 | 多次随机生成 | 连续多次点击并确认 | 每次生成新配置；存在人工编辑时每次先确认；不提供“恢复上一套”能力 |
| EB-043 | 固定区域 | 滚动长表单 | 顶部导航和底部操作栏固定；阴影可见；正文首尾不被遮挡 |
| EB-044 | 业务标注隔离 | 显示交互标注并展开各区块 | 仅业务区和本页操作栏出现 1–16；系统框架无编号；开合后位置重算 |
| EB-045 | 标注默认状态 | 重新打开页面 | 页面无标注徽标；右侧浮标文案为“显示标注”，点击后才显示 |
| EB-046 | 代理搜索与排序 | 搜索代理并切换序号、到期时间排序 | 搜索框宽 400px；排序方向和数据顺序一致，分页回到有效页 |
| EB-047 | 代理信息刷新 | 点击代理信息后的刷新图标 | 仅当前行显示加载并刷新检测信息，选中项不变 |
| EB-048 | 购买代理 | 点击“购买代理” | 新标签页打开商城模块，换绑代理弹窗状态保留 |
| EB-049 | 添加代理 | 分别添加自有代理和代理 API | 二级弹窗字段完整；添加刷新列表；添加并绑定同步更新草稿和状态 |
| EB-050 | 账号绑定列表 | 添加账号后修改并解绑 | 列表显示平台、名称、账号和操作；修改回填；解绑只移除当前绑定 |
| EB-051 | 账号弹窗切换 | 在两个账号弹窗中点击对方入口 | 当前弹窗先关闭，再打开目标弹窗；页面上始终只有一个账号弹窗 |
| EB-052 | 已有账号工具栏 | 打开选择已有账号弹窗 | 无平台、地区、状态筛选；仅显示添加入口、400px 搜索框和刷新按钮 |

### 13.2 可访问性

- 表单控件具有可见 label；
- 分段控件使用 radiogroup/radio；
- Checkbox、按钮和折叠状态可被读屏读取；
- 错误、成功、网络结果通过 live region 宣告；
- 所有文字和背景对比度满足 WCAG AA；
- 焦点样式清晰，弹层关闭后焦点返回触发控件；
- 触控目标至少 32×32px，主要按钮至少 40px 高。

## 14. 验收标准

1. 页面完整覆盖本 PRD 中所有实测字段和选项，不得遗漏折叠区；
2. 名称 0、100、101 字边界行为与规则一致；
3. 换绑代理以独立弹窗呈现，默认本地网络；四类代理具有对应操作、筛选、表格、空态、分页和按钮状态，状态参数只允许“全部状态、未绑环境、已绑环境”；
4. 添加/编辑账号与选择已有账号均以弹窗完整演示，弹窗可互相切换；绑定结果列表支持修改和解绑，敏感字段无泄漏；
5. 基础设置与全部 28 类指纹配置可操作并实时更新摘要；
6. 插件分组双栏面板、全局插件菜单与 8 类偏好设置可操作；浮层不裁切、不重叠，继承值清晰；
7. 随机生成覆盖基础、指纹、插件和偏好配置，不影响环境信息，支持多次生成、覆盖确认；命令区不渲染进度条、“设备画像一致”文字和“恢复上一套指纹”按钮，生成后不提供撤销能力；
8. 保存、取消、离开保护、冲突和失败态均可演示；
9. Mock API 可切换成功、失败、超时、空数据、无权限和并发冲突；
10. 无控制台错误；无敏感信息进入日志、URL、埋点或本地持久化；
11. 键盘操作、焦点管理、错误宣告符合可访问性要求；
12. 视觉严格复用 `design.md` token 与系统框架，不自造颜色、圆角或导航结构。
13. 页面表单满足 120px 标签列、500px 控件区、单字段单行和 16px 字段间距；代理与账号弹窗控件宽 400px，并按各自标签列对齐；安全/风险图标与顶部图例一致。
14. 顶部导航和底部操作栏固定且阴影方向正确；主要业务区块无外描边，仅“自定义指纹配置”区块保留浅蓝描边。
15. 交互标注默认隐藏，右侧浮标初始文案为“显示标注”；显示后仅覆盖编辑浏览器业务内容及本页操作栏，并连续编号为 1–16。

## 15. 实测发现与改进项

| 优先级 | 发现 | 风险 | PRD 决策 |
|---|---|---|---|
| P1 | 随机生成新指纹实测立即覆盖多项值，无确认 | 手动配置被误覆盖 | 有手动修改时确认；不提供撤销或“恢复上一套指纹”能力，已覆盖草稿无法回退 |
| P1 | 取消实测直接丢弃草稿返回列表 | 用户误丢数据 | 推荐启用 dirty 离开确认；保留兼容开关 |
| P1 | 账号区包含密码、2FA、Cookie 等敏感数据 | 日志或权限泄漏 | 全链路脱敏、禁止埋点、字段级权限和审计 |
| P2 | 名称允许超过 100 字后再报错 | 用户可能误认为已截断 | 保留输入并显示明确错误及剩余/超出数 |
| P2 | “跟随团队”未直接说明最终值 | 用户无法预测启动行为 | 展示“跟随团队（最终值）” |
| P2 | 基于 IP 的摘要仅显示策略 | 难判断实际时区/语言 | 网络检测后同时显示解析值 |
| P2 | 代理工作台包含二级添加弹窗，账号存在两个可互相切换的弹窗 | 浮层叠加或临时状态误提交 | 固定标题与底栏；明确浮层优先级；账号弹窗互斥；确定后仅写入浏览器草稿 |
| P3 | 全局插件默认仅展示图标 | 难判断插件名称 | 保留紧凑图标，通过省略号菜单展示单行插件名称；不恢复旧插件卡片列表 |
| P3 | 风险图标依赖颜色 | 色觉障碍用户难区分 | 增加文本、图标形状和读屏描述 |
| P2 | 原生插件分组下拉无法表达团队/个人范围 | 分组归属难理解 | 使用团队分组/我的分组双栏面板，空集合明确展示“暂无数据” |
| P2 | 绝对定位插件菜单可能被区块裁切 | 布局错乱、后续字段重叠 | 菜单参与文档流，区块随交互自动增高并重算标注位置 |

## 16. 非目标范围

- 代理购买与计费流程；
- 自有代理和代理 API 的批量导入、批量删除及供应商结算；
- 账号平台库管理与批量导入；
- 插件安装、上传、审核与更新；
- 浏览器启动器、内核运行时和真实反指纹算法实现；
- 团队权限后台配置；
- 环境列表、回收站及批量操作；
- 自动化/RPA 任务配置。

本页实现代理与账号的单条新增、编辑、选择和绑定；非目标能力只提供跨模块入口或接口适配契约。

## 17. 补充说明

- 实测网络检测成功返回出口 IP 与地区；PRD 示例和原型数据必须使用保留测试地址，不能复用真实 IP、账号、邮箱、手机号或代理信息。
- 实测“随机生成新指纹”会改变硬件并发数、设备内存、设备名称和 MAC 地址，说明该按钮生成的是成组画像；实现不可逐字段无约束随机。
- Chromium 列表包含 beta 与多个历史版本，前端必须由配置驱动，以免版本更新后修改代码。
- 本文档结构已与《Vibe Coding产品PRD模版》同步：不单设用户故事章节，并按“页面整体布局、模块布局、字段与交互”组织页面规格。
