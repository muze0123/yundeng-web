# 飞书归档执行说明

> 状态：本地交付物已完成；2026-08-13 远端执行因外部审批网关返回 `502 Bad Gateway` 未完成。本文只记录下一次恢复执行所需的安全步骤，不代表已经写入飞书。

## 目标

- 父目录：`https://my.feishu.cn/wiki/RQQNwt8V2ioK7ikxY0xcnbIsnHb?from=from_copylink`
- 新建文档：`YunLogin 与 AdsPower 竞品分析报告`
- 新建文档：`竞品分析模版`
- 附件：`YunLogin与AdsPower竞品对比矩阵.xlsx`
- 图片：YunLogin 产品架构图、AdsPower 产品架构图、YunLogin 用户旅程图

## 执行原则

- 显式使用 `--as user`。
- 先解析目标 URL，不能把 Wiki URL 或 node token 当作 `space_id`。
- 新建文档首次写入使用正常样式。
- 如发现同名已有文档，不覆盖；停止并按“已有文档后续修改使用紫色文字和删除线”的规则处理。
- 写前 dry-run，写后回读标题、正文、目录归属、warnings 和 revision。

## 建议步骤

1. 解析目标节点：

   ```bash
   lark-cli wiki +node-get --as user --node-token 'https://my.feishu.cn/wiki/RQQNwt8V2ioK7ikxY0xcnbIsnHb?from=from_copylink'
   ```

2. 使用返回的 `space_id` 与 `node_token` 列出子节点，确认不存在同名文档：

   ```bash
   lark-cli wiki +node-list --as user --space-id '<space_id>' --parent-node-token 'RQQNwt8V2ioK7ikxY0xcnbIsnHb' --page-all
   ```

3. 分别执行 `wiki +node-create --dry-run`，再创建两个 Docx 节点。

4. 为飞书写入准备临时发布副本：把报告中的 Excel 本地链接和三处图片 Markdown 替换为唯一文字锚点。保留本地正式版不变；不得直接把本地路径写进飞书，也不得上传含手机号、IP、MAC、团队 ID 等敏感截图。

5. 首次写入正文：

   ```bash
   lark-cli docs +update --as user --doc '<report_doc_token>' --command append --doc-format markdown --content @./YunLogin与AdsPower竞品分析报告-飞书临时发布版.md
   lark-cli docs +update --as user --doc '<template_doc_token>' --command append --doc-format markdown --content @./竞品分析模版.md
   ```

6. 按唯一文字锚点将三张已脱敏图片插入为飞书图片块，并将 Excel 作为报告文档附件插入；插入成功后删除锚点。图片插入参数以当次 `lark-cli docs +media-insert --help` 为准，附件示例：

   ```bash
   lark-cli docs +media-insert --as user --doc '<report_doc_token>' --type file --file './outputs/019ff91a-00b9-79b3-a937-14cf1ab77e9f/YunLogin与AdsPower竞品对比矩阵.xlsx' --file-view card
   ```

7. 回读两个文档并重新列出父目录子节点，核验标题、正文首尾、三张图片、Excel 附件、warnings、revision 和目录归属。任何图片或附件仍显示为本地路径/占位文本时，不得宣称归档完成。

## 本地文件

- `YunLogin与AdsPower竞品分析报告.md`
- `竞品分析模版.md`
- `outputs/019ff91a-00b9-79b3-a937-14cf1ab77e9f/YunLogin与AdsPower竞品对比矩阵.xlsx`
